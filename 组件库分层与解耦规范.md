# 组件库分层与解耦规范

> 适用对象：接手 `@yd/ui` 的前端、做代码评审的人、以及被接进编辑器里的 AI 助手。
> 这份文档回答一个问题：**一段代码该放哪一层，凭什么这么判，怎么保证它不会烂掉。**

---

## 1. 为什么要分两层

组件库最容易烂掉的方式不是写错代码，而是**边界模糊**：样式既在组件里又在全局 CSS 里，
改一处忘了另一处，两层视觉慢慢分叉 —— 而分叉是**静默的**，两边页面各自都「看起来正常」。

所以这里不靠约定，靠三件事：

1. **物理分层** —— 目录就是边界，`core/` 与 `vue/` 是两棵独立的树；
2. **单向依赖** —— 方向只有一条，反过来就是违规；
3. **会失败的门禁** —— 每条规则对应一个命令，违反即退出码非 0。

分层带来的直接收益：**原生 HTML、静态页、其它框架可以不装 Vue 直接用这套视觉**，
而它们和 Vue 应用看到的界面对齐 —— 因为 CSS 只有一份。

---

## 2. 两层结构

| | `core/` —— 零框架层（Tier 0） | `vue/` —— 组件层（Tier 1） |
| --- | --- | --- |
| 入口 | `@yd/ui/core` | `@yd/ui/vue` |
| 谁在用 | 原生 HTML / 静态单文件 / Vue 应用 / 其它框架 | 只服务 Vue 3 应用 |
| 依赖 | **不依赖任何框架**（连 Vue 都没有） | 依赖 `core` + `vue` + `ant-design-vue` |
| 内容 | 样式三层、41 个图标符号、4 张素材、控件语义映射表、令牌真源 | 18 个 SFC |
| 形态 | 纯 CSS + 纯 TS 数据表 | 组件模板 |

聚合出口 `@yd/ui`（`src/index.ts`）同时导出两层，`import { Modal } from '@yd/ui'` 即可。
只想用样式和类名时**不要**引这个出口 —— 引 `@yd/ui/core`，或直接用 `dist-lite/`。

### core 层里有什么

```
src/core/
  styles/     tokens.css → base.css → components.css   ← 顺序即级联，不可乱
  icons/      icons.ts        41 个 SVG 符号的字符串表
  assets/     kpi-*.png       4 张配图（随库发布，库自包含）
  contract/   tag-class.ts    状态词 → 类名映射（状态词取值空间的唯一真源）
              tree.ts         权限树类型与工具
              assets-manifest.ts  素材键 → 文件名（纯数据，无打包器也能读）
              skin-hooks.ts   皮肤钩子登记表（见 §6）
  theme/      tokens.ts       设计令牌真源 → 生成 styles/tokens.css
```

### vue 层里有什么

18 个 SFC：`Icon` `IconTile` `SearchBox` `EmptyState` `StatusTag` `UrgencyTag` `KpiImg`
`Modal` `OverflowTooltip` `Pagination` `PermissionTree` `PermissionTreeRows`，
以及 P3 从业务工程抽来的 `PortalMenu` `SearchSelect` `SingleSelect` `SelectCard` `ChipTree` `HoverCard`。

**它们全部不含 `<style>` 块。** 组件只做两件事：拼类名、管行为。

---

## 3. 依赖方向铁律

```
        ┌──────────────────────────┐
        │  @yd/ui/core  （Tier 0）  │
        └──────────────────────────┘
                    ▲
                    │   只有这一个方向是合法的
                    │
        ┌──────────────────────────┐
        │  @yd/ui/vue   （Tier 1）  │
        └──────────────────────────┘
```

### 六条硬规则

| 编号 | 规则 | 守它的检查 |
| --- | --- | --- |
| **C1** | `core/` 不得 `import` `vue` 或 `ant-design-vue` | `check:layering` |
| **C2** | `core/` 里不得出现 `.vue` 文件，也不得 `import` `.vue` | `check:layering` |
| **C3** | 任何库文件不得反向依赖宿主（相对路径不得越出库根；不得用宿主别名 `@/`） | `check:layering` |
| **V1** | `vue/` 下的组件不得带 `<style>` 块 | `check:layering` |
| **V2** | 组件模板里出现的**库家族类名**必须在 `class-contract.json` 内（或是已登记的皮肤钩子） | `check:layering` |
| **A1/A2** | 契约登记的资产必须真实存在、三层样式齐全且顺序正确；登记的皮肤钩子必须真被模板用到 | `check:layering` |
| **D1** | `docs-site/` 的 import 只能来自库自身 / 已登记的 peer / 相对路径；不得引入 lodash 之类未登记的三方包 | `check:layering` |
| **D2** | `docs-site/` 不得反向依赖宿主或本机：源码里不得出现宿主端口 `5173`、`YD_UI_DIR` 环境变量、或任何写死的绝对路径（Windows 盘符 / POSIX 家目录） | `check:layering` |

另有四条项目级禁令（R1–R4），其中 **R4 = core 不得依赖 vue 层**，是 C1/C2 的通俗说法。

### 为什么还要 D1/D2（文档站不能是宿主的尾巴）

C1–C3 守的是「库的源码」不越界，但文档站曾经住在宿主里、跑在宿主的 5173 上，
于是出现一条**反向依赖**：库要跑自己的验收（`vanilla-parity`），得先把宿主的开发服务器点起来。
这种依赖不会让编译失败，却让「这套库能独立交付给别人」这句话站不住 ——
别人把库拷走，文档站和验收脚本全趴窝。

所以文档站（`docs-site/`）连同构建档案（`docs/`、`scripts/`）整体搬进库内，
并新增 **D1/D2** 两条门禁把「文档站不依赖宿主」钉死。
现在库自带 `pnpm install` + `npm run docs`，`http://localhost:5174/` 就起来了，与宿主无关。

### 为什么 C1 值得单列

因为「顺手 import 一下」是最容易发生的越界，而且后果最重：
一旦 core 依赖了 Vue，core 就不再是零框架层，原生 HTML 路径直接失效 ——
但**编译不会报错**，只有人真的去用一个不装 Vue 的项目时才会暴露。
所以它必须由机器守，且必须能证明机器守得住（见 §8）。

---

## 4. 准入判据：一段代码该放哪一层

这是整份文档最实用的部分。按顺序问四个问题，第一个「是」就是答案。

### ① 它是「控件语义」还是「页面语义」？

- **控件语义** → 进库。判据：换个页面它依然成立。按钮长什么样、标签有几种配色、树的一行怎么缩进 —— 都是控件语义。
- **页面语义** → 留宿主。判据：它绑定了某个具体页面的上下文。`.approval-actions .btn` 这种「审批页里的按钮要窄一点」是页面语义。

### ② 它是「样式」还是「行为」？

- **样式** → 可以进 `core/`（写成类名配方）。
- **行为** → 进 `vue/`，或者干脆不进库。

### ③ 去掉 Vue 之后，它成立吗？

- **成立** → `core/`。
- **不成立** → `vue/`。

### ④ 它是不是业务规则？

- **是** → 留宿主。阈值、状态机、权限判定这类东西一旦进库，库就被某个业务绑死了。
  库只提供控件语义，不提供业务判断。

### 落到目录

| 你手上是 | 放哪 | 例子 |
| --- | --- | --- |
| 一条控件的外观 | `core/styles/components.css` 的对应族 | `.tag-pending` 的配色 |
| 一份纯数据映射表 | `core/contract/` | 状态词 → 类名 |
| 一张随组件用的图 | `core/assets/` + 登记到 `assets-manifest.ts` | KPI 配图 |
| 一个需要递归 / 状态 / 事件的东西 | `vue/components/` | `PermissionTree` 的展开态 |
| 某个页面专用的偏移与覆盖 | 宿主 `pages.css` | `.approval-actions .btn` |
| 业务阈值 / 权限判定 | 宿主 `shared.ts` | `riskLevel(3天/7天阈值)` |

### 两个真实的反例，比规则更好记

**① 标签页（`.tabs` / `.tab`）在 core，但**没有** Vue 组件。**
原生页面里切换标签页通常交给后端路由（`/list?tab=2`，整页重渲染），用不着 Vue 接管。
硬写一个组件反而多余 —— 所以它的类名住在 `core/`。
**这说明 core 是 vue 的超集**：用 vue 层能拿到的视觉，用 core 层的类名一样能拿到；反过来不成立。

**② 溢出提示（`OverflowTooltip`）在 vue，而且 core 层**不应该**有等价物。**
「文本是否真的被省略号截断」要测量 DOM（`ResizeObserver` + 双帧 rAF），
这是**行为**不是样式。把它做进 core，core 就不再零框架。
所以原生侧只能退到 `title` 属性 —— 这是静默降级，也是正确的边界。

---

## 5. 类名契约：两层唯一的接口

两层的视觉之所以是同一份，是因为它们共享**同一个接口：类名**。

- 真源：`src/core/styles/components.css`（**人改这里**）
- 产物：`schema/class-contract.json`（**机器生成，别手改**）
  —— 27 个家族 / 128 个类名 / 19 个状态修饰类 / 13 个状态词 / 41 个图标键 / 4 个素材键
  来源 `206` 条规则，其中 `4` 条在 `@media` 里

```bash
npm run build:contract     # 从 CSS 真源重新生成契约
```

契约同时被这些消费者读取，所以它们不可能对不上：

| 消费者 | 用契约做什么 |
| --- | --- |
| 门禁 `check-layering` | V2：判断模板里的类名是否合法 |
| 文档站 | 展示可用类名与状态词表 |
| AI 助手 | 类名白名单（避免「引用不存在的类名」这类静默失效） |
| 原生 HTML 作者 | 查表，而不是凭感觉拼 |

**契约生成时会把 CSS 里所有类名归到家族；当前未归属类名为 0** ——
也就是说没有「写了样式但没人知道它属于谁」的死类。

---

## 6. 皮肤钩子：唯一被允许的跨边界耦合

有一处类名是**库输出、宿主给样式**的：

| 类名 | 样式来自 | 为什么这样办 |
| --- | --- | --- |
| `tree-toolbar-btn` | 宿主 `pages.css` | 权限树工具条按钮的皮肤由具体页面决定，不属于控件配方 |

这种耦合**藏起来最危险**，所以它被显式登记在 `core/contract/skin-hooks.ts`，
并且门禁同时守两头：

- **A2**：登记了但模板没用到 → 失败（防止登记表烂掉）
- **V2**：模板用了契约外又没登记的类名 → 失败

**新增一个皮肤钩子，必须同时改三处**：`skin-hooks.ts`、`class-contract.json`（重新生成）、
以及组件模板。少任何一处，门禁都会拦住。

> 设计取向：皮肤钩子是**例外**，不是逃生口。每加一个都要在这里留一行说明。
> 登记表长了，说明边界划错了 —— 那就回去改 §4 的判据，而不是继续加。

---

## 7. 两条消费路径

### 路径 A：Vue 应用

```ts
@import '@yd/ui/core/styles/tokens.css';
@import '@yd/ui/core/styles/base.css';
@import '@yd/ui/core/styles/components.css';

import { Modal, StatusTag, PermissionTree } from '@yd/ui'
```

`peerDependencies` 里 `vue` 与 `ant-design-vue` 都标成了 **optional** ——
因为只有 `vue/` 层需要它们。

### 路径 B：原生 HTML / 零构建

不装依赖、不跑构建。库把三层 CSS 合并成一个文件，连同一份 sprite 与素材，放在 `dist-lite/`：

```html
<link rel="stylesheet" href="./dist-lite/yd-ui.css">

<svg width="18" height="18" viewBox="0 0 24 24">
  <use href="./dist-lite/icons.svg#yd-icon-shield"/>
</svg>

<img class="kpi-asset" src="./dist-lite/assets/kpi-pending.png" width="40" height="40" alt="">

<button class="btn btn-primary">提交</button>
```

| 产物 | 内容 |
| --- | --- |
| `dist-lite/yd-ui.css` | tokens + base + components 三层合并（顺序即级联） |
| `dist-lite/icons.svg` | 41 个符号，id 前缀 `yd-icon-` |
| `dist-lite/assets/` | 4 张 PNG |
| `dist-lite/manifest.json` | 哈希、图标 id 清单、素材文件名 —— **查这个，不要猜** |

> **`dist-lite/` 是提交进仓库的构建产物。** 因为原生消费者不跑构建，产物必须在仓库里现成可用。
> 改完 `core/styles/` 必须重建，否则直引的那一侧就悄悄落后了 —— 这正是 `check:lite` 要拦的情况。

### core 层没有的东西

**core 层只有样式，一条 JS 都没有。** 这是刻意的：交互逻辑一旦进 core，core 就不再零框架；
而每个原生页面的交互需求各不相同（原生 / jQuery / 服务端渲染），库没法替它们决定。

| 需求 | core 层 | 原生侧怎么做 |
| --- | --- | --- |
| 弹窗开关 / Esc 关闭 | 只有结构 | 容器 `hidden` 属性 + 两三行事件绑定 |
| 树 / 折叠展开 | 只有结构 | 原生 `<details>` / `<summary>`，类名照旧 |
| 分页翻页 | 只有结构 | 后端路由（`?page=2`）或自行绑定事件 |
| 溢出才弹的提示 | **没有，且不应有** | `title` 属性静默降级，或引 `vue/` 层 |

⚠️ 一个必须知道的坑：库给 `.modal-backdrop` 设了 `display:flex`，
它会盖过 `hidden` 属性的默认样式 —— 原生页面里要自己补一条 `[hidden]{display:none}`。

---

## 8. 门禁：一组命令

分层靠自觉是守不住的，所以每条规则都对应一个会失败的命令。

```bash
npm run check          # = 静态门禁一起跑（tokens / schema / layering + 自检 / lite）
```

静态门禁（跑一次几秒，进 CI 必跑）：

| 命令 | 守什么 | 怎么发现问题 |
| --- | --- | --- |
| `node scripts/check-layering.cjs` | C1/C2/C3 + V1/V2 + A1/A2 + **D1/D2** | 静态扫描 + 逐条断言（8 项） |
| `node scripts/check-layering.selftest.cjs` | 门禁自己是否真会失败 | 合成副本灌入每种违规，断言确实失败 |
| `node scripts/build-lite.mjs --check` | dist-lite 与 core 源码是否同步 | 比对 sha256，不同步退出码 1 |
| `node scripts/check-tokens.cjs` · `check-schema.cjs` | 令牌真源 / 契约与源码是否一致 | 逐条比对，漂移即失败 |

浏览器门禁（要起服务，验收时才跑）：

| 命令 | 守什么 | 怎么发现问题 |
| --- | --- | --- |
| `node scripts/vanilla-parity.cjs` | 原生渲染与 Vue 渲染的计算样式是否一致 | 真浏览器开两页，25 组选择器 × 26 属性逐项比对 |
| `node scripts/vanilla-probe.cjs` | 原生示例页自身是否成立 | 零报错、sprite 可解析、各族非零渲染、语义配色不混淆 |
| `node scripts/docs-probe.cjs` | 文档站自身是否成立 | 导航覆盖契约组件、示例可交互、无控制台报错 |

> 这两页服务都住在库内：文档站 `npm run docs`（5174）、原生示例 `npm run serve:vanilla`（5175），
> 一条 `node scripts/verify-browser.cjs` 就能把上面三个浏览器门禁串起来跑完。

### 门禁自己也要被验证

一道从来不会失败的门禁，等于没有门禁。所以 `check-layering` 带一个自检：

```bash
node scripts/check-layering.selftest.cjs      # → 22/22 通过
```

它用一个**合成副本**（不是真库）主动灌入每一种违规 —— core 里 import vue、core 里放 `.vue`、
越出库根的相对路径、宿主别名 `@/`、组件带 `<style>`、模板用契约外的类名、三层顺序被改动、
陈旧皮肤钩子、文档站 import 三方包（D1）、文档站里写宿主端口/路径（D2） ——
然后断言门禁**确实失败**，并且不误伤正常写法。
（其中两条专门验证「不该炸的别炸」：注释里写了 `import 'vue'` 不算违规；
`<pre>` 代码示例里出现 `5173` 也不算违规 —— 文案不该把门禁弄炸。）

### 为什么 parity 是关键那一道

分层的全部价值压在一个断言上：

> **core 的类名配方，就是 vue 组件的视觉。没有第二份定义。**

一旦有人给组件加了 `<style>`，两层就开始分叉 —— 而分叉是静默的。
所以用真实浏览器把同一批类名在两页上的计算样式逐属性比对来钉住它。

已知的两个「假差异」陷阱，脚本里都做了处理，改脚本时别踩回去：

1. `page.goto` **不会**重置鼠标位置，上一页残留的 hover 会污染读数 → 读样式前先把鼠标停到角落；
2. 外层是 flex 还是 block 会改变子元素的 `display` 与 `min-height` 解析 →
   两边演示容器类型必须一致（按钮用块级、`EmptyState` 用块级）。

## 9. 常见违规与修法

| 症状 | 真正的原因 | 怎么修 |
| --- | --- | --- |
| 原生页面「差了点样式」 | `dist-lite` 与源码不同步 | `npm run build:lite` |
| 图标空白，但控制台没报错 | `<use href>` 的 id 写错（拼错不会报错） | 查 `dist-lite/manifest.json` 的 `icons` |
| 配图 404 但页面「看起来没事」 | 素材键拼接错误 | 查 `manifest.json` 的 `assets`（只有 4 个键） |
| 页面样式被页面级覆盖「压不住」 | 三层 CSS 的顺序反了 | 恢复 tokens → base → components 顺序，排在宿主 `pages.css` 之前 |
| 弹窗 `hidden` 不起作用 | 库给了 `display:flex`，盖过了 `hidden` | 页面里补 `[hidden]{display:none}` |
| 同一个视觉在两个页面不一样 | 有人给组件加了 `<style>` | 删掉，样式补进 `core/styles/components.css` 对应族 |
| 改完类名后门禁红了 | 契约没重新生成 | `npm run build:contract` |
| 想给组件加个皮肤类 | 这是跨边界耦合 | 按 §6 登记成皮肤钩子，别藏在模板里 |

---

## 10. 新增东西的标准流程

### 新增一个组件

1. 按 §4 判据确认它属于 `vue/`（若纯样式，直接进 `core/styles/`，不建组件）。
2. 在 `src/vue/components/` 写 SFC，**不写 `<style>`**，需要的外观先去 `components.css` 找现成类名。
3. 若需要**新类名**：先加到 `core/styles/components.css` 的对应族，再 `npm run build:contract`。
4. 从 `src/vue/index.ts` 导出。
5. 跑 `npm run check:layering`（V1/V2 会立刻告诉你类名是否越界）。
6. 跑 `node scripts/build-lite.mjs` 重建直引包。
7. 跑 `node scripts/vanilla-parity.cjs`，确认两层仍一致。
8. 在 `schema/components.json` 补元数据：props / events / slots / demos / **native**（原生侧有没有等价写法）。

### 新增一个纯样式配方（不建组件）

1. 在 `core/styles/components.css` 加规则（注意归入已有族或新建族）。
2. `npm run build:contract`。
3. `node scripts/build-lite.mjs`。
4. 在 `examples/vanilla/index.html` 补一段演示 —— **原生示例页是这一层的验收面**。
5. `node scripts/vanilla-probe.cjs` 会检查「每个组件族都真有渲染」，
   新族没有演示会直接失败（这条检查就是为此存在的）。

### 改样式

改 `core/styles/` → `build:contract`（若类名/家族变了）→ `build:lite` → `vanilla-parity` + `vanilla-probe`。
**永远不要直接改 `dist-lite/yd-ui.css`** —— 它是产物。

---

## 11. 文件地图

```
组件库/
  src/
    index.ts              聚合出口（两层都导出）
    core/                 零框架层（Tier 0）—— 不依赖任何框架
      index.ts
      styles/             tokens.css → base.css → components.css → index.css（合并入口）
      icons/icons.ts
      assets/             kpi-*.png
      contract/           tag-class.ts · tree.ts · assets-manifest.ts · skin-hooks.ts
      theme/tokens.ts     令牌真源
    vue/                  组件层（Tier 1）—— 18 个 SFC，不含 <style>
      index.ts
      components/*.vue
  schema/
    components.json       组件契约（props/events/slots/demos/native）—— 文档站的数据源
    class-contract.json   类名契约（自动生成，勿手改）
  dist-lite/              原生直引包（构建产物，但提交进仓库）
    yd-ui.css · icons.svg · assets/ · manifest.json
  examples/vanilla/       零构建原生示例页 —— core 层的验收面
  docs-site/             ★ 文档站（本仓库自带，独立跑在 5174，不依赖宿主）
    index.html · main.ts · DocsApp.vue · site.css · vite.config.ts
  docs/                  ★ 构建档案（迁移报告 / 分层决策记录，7 篇）
  scripts/
    build-contract.mjs            生成类名契约
    check-layering.cjs            分层门禁（C1/C2/C3/V1/V2/A1/A2 + D1/D2）
    check-layering.selftest.cjs   给门禁灌违规，证明它真会失败
    docs-probe.cjs                文档站验收
    docs-inspect.cjs              文档站结构抽查
    sfc-lint.cjs                  SFC 结构检查
    verify-browser.cjs            一条龙起服务 + 跑三个浏览器门禁
    migration/                   一次性迁移脚本（archive，已用完，留档）
    build-lite.mjs                生成 dist-lite（--check 守同步）
    vanilla-parity.cjs            两层计算样式逐属性比对
    vanilla-probe.cjs             原生示例页验收
    serve-vanilla.mjs             零依赖静态服务器（本地看示例页）
    p2-split-layers.mjs           一次性迁移脚本（P1 扁平结构 → 两层），已用完
  ai/                            AI 接入物（Copilot / Cursor / Claude）
  llms.txt                       给 LLM 的压缩说明
```

---

## 12. 一页速记

- **两个层**：`core/` 零框架，`vue/` 组件。
- **一个方向**：`vue → core`。反过来就是违规。
- **一个接口**：类名。真源 `core/styles/components.css`，产物 `class-contract.json`。
- **一条铁律**：组件不写 `<style>`。写不出样式，说明该补 `components.css`，不是该写内联。
- **一条边界**：core 只有样式，没有 JS。要行为的去 vue 层，或者自己写。
- **一个例外**：皮肤钩子（`tree-toolbar-btn`），登记在案，且必须登记。
- **四道门**：`check:layering`（8 项，含 D1/D2 文档站不依赖宿主） · `check:lite` · `vanilla-parity` · `vanilla-probe`。
- **一个自包含**：文档站（`docs-site/`）、构建档案（`docs/`）、验收脚本（`scripts/`）都住在库内。拷走这个仓库，`pnpm install && npm run docs` 就起来了，不需要宿主。
- **改完样式**：`build:contract`（若类名变了）→ `build:lite` → 跑 parity 与 probe。
