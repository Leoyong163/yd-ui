# P0-T4 删除候选逐条确认

> 由 `scripts/p0-dead-verify.cjs` 生成。对 `src/**` 全部 `.vue/.ts/.js/html` 做全文词面搜索。

## 结论

| 判定 | 数量 | 说明 | 处置 |
| --- | --- | --- | --- |
| SAFE_TO_DELETE | 139 | 全文无任何引用，且无拼装前缀 | 可删 |
| ALIVE_LITERAL | 42 | 在 JS/模板串里字面出现（探测器漏掉） | **不可删** |
| ALIVE_DYNAMIC | 2 | 由模板串按前缀拼装 | **不可删** |
| ANT_RUNTIME_STYLED | 2 | antd 运行时生成的类名，且我们的 CSS 在给它上样式 | **绝对不可删** |
| ANT_RUNTIME_UNSTYLED | 21 | antd 运行时类名，但未发现对应样式 | 保留（无害） |

## 各样式表分布

| 样式表 | SAFE_TO_DELETE | ALIVE_LITERAL | ALIVE_DYNAMIC | ANT_RUNTIME_STYLED | ANT_RUNTIME_UNSTYLED |
| --- | --- | --- | --- | --- | --- |
| components.css | 79 | 10 | 2 | 1 | 11 |
| pages.css | 51 | 25 | 0 | 1 | 10 |
| layout.css | 5 | 6 | 0 | 0 | 0 |
| base.css | 3 | 0 | 0 | 0 | 0 |
| theme.css | 0 | 1 | 0 | 0 | 0 |
| animations.css | 1 | 0 | 0 | 0 | 0 |

## ALIVE_LITERAL（42）—— 不可删

| 类名 | 定义于 | 置信 | 证据（原文片段，供人工复核） |
| --- | --- | --- | --- |
| `current` | components.css | 高（同串还有别的类名） | src/pages/Approval.vue: item${item.id === current?.id ? ' active' : |
| `error` | components.css | 待确认（可能只是同名词） | src/pages/role-form/ExpandedRowDetails.vue: orderBottom: item.error ? '1px solid #ff4 |
| `highlight` | components.css | 待确认（可能只是同名词） | src/pages/Home.vue: 'highlight' |
| `nowrap` | components.css | 待确认（可能只是同名词） | src/components/OverflowTooltip.vue: : 0; white-space: nowrap; overflow: hidden |
| `pending` | components.css | 待确认（可能只是同名词） | src/pages/approval-types.ts: 'pending' |
| `required` | components.css | 待确认（可能只是同名词） | src/pages/ApplyForm.vue: "[{ required: true }]" |
| `success` | components.css | 待确认（可能只是同名词） | src/pages/Home.vue: 'success' |
| `tab` | components.css | 高（同串还有别的类名） | src/pages/ApplyForm.vue: opdown-tab${t === tab.value ? ' is-acti |
| `textarea` | components.css | 待确认（可能只是同名词） | src/devtools/Inspector.vue: 'textarea' |
| `toast` | components.css | 待确认（可能只是同名词） | src/devtools/Inspector.vue: "toast" |
| `apply-cat-item` | pages.css | 高（同串还有别的类名） | src/pages/Apply.vue: "`apply-cat-item${category === c ? |
| `approval-list-item` | pages.css | 高（同串还有别的类名） | src/pages/Approval.vue: lection-list-item approval-list-item${item.id === curr |
| `approval-role-perm-trigger` | pages.css | 高（同串还有别的类名） | src/pages/ApprovalRolePermHoverCard.vue: "`approval-role-perm-trigger${open ? ' is-open |
| `days-badge` | pages.css | 高（同串还有别的类名） | src/pages/Expiring.vue: "`days-badge ${r.remainingDays |
| `expiring-role-item` | pages.css | 高（同串还有别的类名） | src/pages/Expiring.vue: ization-list-item expiring-role-item${r.id === current |
| `form-mini-select` | pages.css | 高（同串还有别的类名） | src/pages/role-form/ExpandedRowDetails.vue: -dropdown-wrapper form-mini-select${openSubIte |
| `form-radio-option` | pages.css | 高（同串还有别的类名） | src/pages/AgentAuthForm.vue: "`form-radio-option${opt === formStat |
| `form-select-chevron` | pages.css | 待确认（可能只是同名词） | src/pages/role-form/FormSelect.vue: "form-select-chevron" |
| `form-select-dropdown` | pages.css | 待确认（可能只是同名词） | src/pages/role-form/FormSelect.vue: "form-select-dropdown" |
| `highlight` | pages.css | 待确认（可能只是同名词） | src/pages/Home.vue: 'highlight' |
| `identity-stat` | pages.css | 待确认（可能只是同名词） | src/pages/Home.vue: "`identity-stat ${s.cls}`" |
| `item-role` | pages.css | 待确认（可能只是同名词） | src/pages/IDaaSRoleForm.vue: "item-role" |
| `item-sys` | pages.css | 待确认（可能只是同名词） | src/pages/IDaaSRoleForm.vue: "item-sys" |
| `item-text` | pages.css | 待确认（可能只是同名词） | src/pages/IDaaSRoleForm.vue: "item-text" |
| `permission-risk-label` | pages.css | 待确认（可能只是同名词） | src/pages/Expiring.vue: "`permission-risk-label ${risk!.badge}`" |
| `post-label-date-input-wrap` | pages.css | 高（同串还有别的类名） | src/pages/PostLabelDatePicker.vue: "`post-label-date-input-wrap ${open ? 'focused |
| `purple` | pages.css | 待确认（可能只是同名词） | src/icons.ts: 'purple' |
| `role-auth-dropdown-tab` | pages.css | 高（同串还有别的类名） | src/pages/ApplyForm.vue: `role-auth-dropdown-tab${t === tab.value  |
| `role-single-select` | pages.css | 高（同串还有别的类名） | src/pages/role-form/RoleSingleSelect.vue: "`role-single-select${open ? ' is-open |
| `row` | pages.css | 待确认（可能只是同名词） | src/pages/ApplyForm.vue: "(row, index) in tableR |
| `success` | pages.css | 待确认（可能只是同名词） | src/pages/Home.vue: 'success' |
| `system-pick-item` | pages.css | 高（同串还有别的类名） | src/pages/AgentAuthForm.vue: "`system-pick-item${systemId === s.i |
| `tab` | pages.css | 高（同串还有别的类名） | src/pages/ApplyForm.vue: opdown-tab${t === tab.value ? ' is-acti |
| `textarea` | pages.css | 待确认（可能只是同名词） | src/devtools/Inspector.vue: 'textarea' |
| `unread` | pages.css | 待确认（可能只是同名词） | src/pages/Home.vue: "a.unread && !readIds.has(a |
| `app` | layout.css | 待确认（可能只是同名词） | src/main.ts: '#app' |
| `route-apply-form` | layout.css | 待确认（可能只是同名词） | src/Shell.vue: 'route-apply-form' |
| `route-approval` | layout.css | 待确认（可能只是同名词） | src/Shell.vue: 'route-approval' |
| `sidebar-collapsed` | layout.css | 待确认（可能只是同名词） | src/Shell.vue: 'sidebar-collapsed' |
| `sidebar-open` | layout.css | 待确认（可能只是同名词） | src/Shell.vue: 'sidebar-open' |
| `sidebar-overlay-open` | layout.css | 待确认（可能只是同名词） | src/Shell.vue: 'sidebar-overlay-open' |
| `dark` | theme.css | 待确认（可能只是同名词） | src/App.vue: 'dark' |

## ALIVE_DYNAMIC（2）—— 不可删

| 类名 | 定义于 | 置信 | 证据（原文片段，供人工复核） |
| --- | --- | --- | --- |
| `modal-lg` | components.css | 高 | src/components/Modal.vue 拼装前缀 modal-${…} |
| `modal-xl` | components.css | 高 | src/components/Modal.vue 拼装前缀 modal-${…} |

## ANT_RUNTIME_STYLED（2）—— 不可删

| 类名 | 定义于 | 置信 | 证据（原文片段，供人工复核） |
| --- | --- | --- | --- |
| `ant-form-item-label` | components.css | 高 | — |
| `ant-select-open` | pages.css | 高 | — |

## SAFE_TO_DELETE（139）—— 删除清单

> 删除时按样式表分组进行，每删一张表先跑一次基线比对（`p0-baseline-diff.cjs`）。
> 注意：这些类可能出现在**组合选择器**里（如 `.tag-approved, .tag-active`），删除时只能移除该选择器片段，不能整条规则删掉。

### components.css（79）

```
breadcrumb  btn-danger-ghost  btn-icon  btn-lg  card-footer  cell-node  char-count  check-row  chip  chip-group  done  dot  dragover  drawer  drawer-backdrop  drawer-body  drawer-footer  drawer-full  drawer-header  drawer-lg  dropdown-menu  error-summary  file-progress  file-progress-bar  flow-arrow  flow-diagram  flow-node  form-checkbox-native  form-error  form-field  form-label  full  glass-panel  has-error  icon-3d  icon-3d-blue  icon-3d-cyan  icon-3d-green  icon-3d-lg  icon-3d-orange  icon-3d-purple  icon-3d-rose  icon-3d-sm  icon-moon  icon-sun  info  input-clear  input-wrap  is-loading  leaving  radio-row  segment  segment-item  sep  short  skeleton-block  skeleton-card  skeleton-grid  spinner  stat-card  stat-grid  stat-label  stat-value  switch  switch-track  timeline  timeline-item  timeline-meta  timeline-title  toast-actions  toast-close  toast-content  toast-icon  toast-msg  toast-root  toast-title  top-right  tree-empty  warning
```

### pages.css（51）

```
apply-filter-bar  approval-overview-role-sep  approval-overview-roles  badge-new  banner-art  compact  current-role-bar  current-role-label  current-role-value  detail-actions-row  detail-hero-extra  error-summary  feature-banner  feature-banner-solid  flow-card-footer  flow-card-meta  flow-card-top  form-auth-column-head-title  form-field  form-grid  form-inline-grid  form-section  form-section-title  form-switch-item  form-switch-row  full  link-action  mobile-back  muted  notify-item  notify-list  notify-meta  notify-title  permission-detail-summary  person-avatar  person-picker-list  person-row  role-pick-checkbox  role-selected-item  role-selected-list  selected-chip  selected-chips  shortcut-cat  shortcut-cta  shortcut-icon  shortcut-info  shortcut-item  shortcut-list  shortcut-name  star-btn  toast-root
```

### layout.css（5）

```
bg-particle  nav-chevron  page-loading  sidebar-force-open  two-col
```

### base.css（3）

```
desktop-only  mono  sr-only
```

### animations.css（1）

```
scan-line
```
