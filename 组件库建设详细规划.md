# 系统组件库与设计规范 · 建设详细规划

> 对象系统：权限自助门户（`D:\demo\grok\权限自助门户demo\vue代码包`，Vue 3 + Vite + ant-design-vue）
> 库落地位置：`D:\demo\组件库`
> 上游依据：`docs/组件库拆分可行性评估.md`、`docs/类名归属裁决表.md`、`docs/前端样式与质量体检报告.md`
> 本文性质：**执行规划**（含任务编号、验证门、回滚方式），不是可行性论证

---

## 执行状态

| 阶段 | 状态 | 记录 |
| --- | --- | --- |
| **P0** 准备与裁决 | ✅ **已完成**（2026-09-20） | 见宿主仓库 `docs/P0裁决结论.md` |
| **P1** 库骨架与地基 | ✅ **已完成**（2026-09-21） | 库落地 `D:\demo\组件库`（`@yd/ui` v0.1.0）；宿主 alias 直引源码；12 组件 + 地基已入库；样式三层分层完成 |
| **组件文档站 + AI 接入物** | ✅ **已完成**（2026-09-21，P1 附加交付） | 宿主 `docs.html` + `site/**`（antd/tdesign 风格可视化页）；`schema/components.json` + `llms.txt` + `ai/{claude-skill,copilot-instructions,cursor-rules}` |
| **P2** 两层结构与零构建直引 | ✅ **已完成**（2026-09-21） | 物理分成 `core/`（零框架）与 `vue/`（12 组件）；类名契约与分层门禁；`dist-lite/` 直引包 + 原生示例页；两层一致性实测 |
| **P2b** 库自包含（与宿主隔离） | ✅ **已完成**（2026-09-21） | 文档站（`docs-site/`）+ 构建档案（`docs/`、`scripts/`）迁入库内；门禁加 D1/D2 防反向依赖；库自带 `pnpm install && npm run docs` 跑在 5174；宿主瘦身为纯消费方 |
| P3 ～ P6 | 待开工 | — |

> ⚠️ **P2 改动了目录结构，本文下面的路径已过时。** 旧的扁平路径 → 新路径：
>
> | 文中写的 | 现在的位置 |
> | --- | --- |
> | `foundation/icons.ts` | `core/icons/icons.ts` |
> | `foundation/tag-class.ts` · `tree.ts` · `kpi-assets.ts` | `core/contract/{tag-class,tree,kpi-assets}.ts` |
> | `foundation/assets/` | `core/assets/` |
> | `styles/` | `core/styles/` |
> | `theme/tokens.ts` | `core/theme/tokens.ts` |
> | `components/*.vue` | `vue/components/*.vue` |
> | `@yd/ui/styles/*` | `@yd/ui/core/styles/*` |
>
> 现行规则以 **[组件库分层与解耦规范.md](./组件库分层与解耦规范.md)** 与 `README.md` 为准，
> 本文保留原始规划与当时的判据记录，供追溯，不再逐处回改。

> **P0 有三处结论推翻了本规划的原始判断**（P0裁决结论 §9）：真冲突从 46 条降到 4 条；可删死类从 198 降到 135（23 条 antd 运行时类绝不能删）；反馈族 `toast-root`/`error-summary` 实为无实现的死类，不是「归库」。
> P1 起按修正后口径执行。

> **P1 验证门全过（2026-09-21）**：宿主 `vite build` EXIT=0；基线比对 `DIFF_TOTAL=0`（30 组 / 6630 元素，五类全 0，改配置后二次独立采集仍为 0）；文档站真实浏览器验收 15/15 PASS。
>
> **P1 三处与原规划的偏差**：① 实际拆分结果库侧 199 条配方 / 宿主侧 106 条残留（非原估的 242），组合选择器需拆分 0 条；② 新增必配项——库在项目根之外，`vite.config.ts` 必须加 `PEER_ALIAS` 把 `vue`/`ant-design-vue`/`dayjs` 钉到宿主 `node_modules`，否则 `vite build` 直接失败（Rollup 解析不到 peer dep）；③ 新增必配项——`server.fs.allow` 必须含库目录（跨盘）。
>
> **P1 已暴露的库令牌陷阱**：`--bg-1` / `--bg-2` / `--bg-3` 无暗色覆盖，被当作页面底色时暗色下不反转。已在 README 与 `ai/*` 规则文件的 known gaps 中记录；P2 修令牌层时应一并补暗色覆盖。

---

## 0. 一句话结论

**可行。但真正的第一件事不是"搬组件"，而是"解依赖"**——这个项目的组件外观由全局 CSS 类名决定（48 个 `.vue` 里 0 个写了 `<style>`），且 5 个组件反向 import 了宿主的工具模块与图片资产。所以「搬走组件」会连带拉出一串宿主依赖，必须先把它切断。

---

## 1. 终局目标（Definition of Done）

全部满足才算建成，任一条不满足则该阶段未完成：

| # | 验收标准 | 判定方式 |
| --- | --- | --- |
| 1 | 库可被单独引用：`import { Modal, Pagination } from '@yd/ui'` 即可用，不需引入宿主任何文件 | 建一个只依赖库的最小页面能跑通 |
| 2 | 库源码可增删改，改完宿主即时生效，无需发版、无需手动同步 | 改库内一个组件的字号，宿主页面 3 秒内变化 |
| 3 | 库不反向依赖宿主：库内 `import` 不出现宿主相对路径 | `check-deps` 脚本静态校验，违规即失败 |
| 4 | 设计规范与代码同源：令牌表由 `tokens.ts` 生成，不是手写 | 改令牌后规范页数值同步变化 |
| 5 | 视觉零回归：搬迁前后亮/暗双主题计算样式与截图逐项一致 | 快照比对脚本，差异为空 |
| 6 | 有锁：改名、裸色值、依赖越界会在检查阶段就被拦下 | `npm run check` 覆盖 5 项检查 |

---

## 2. 现状基线（实测台账）

数字来自 `scripts/coupling-probe.cjs` 静态解析，可重跑复核。

### 2.1 体的规模

| 指标 | 数值 |
| --- | --- |
| `.vue` 文件 | 48（`src/components` 12 / `src/pages` 32 / 根与 devtools 4） |
| 含 `<style>` 块的 `.vue` | **0** |
| CSS 类名（去重）/ 行数 | **652** / 7578 行 |
| CSS 文件 | 10（`styles/` 4 + `styles/demo1/` 6） |
| 内联 `style="…"` | **519 处**，其中 **288 处完全重复**（77 组） |

### 2.2 四类脏点

| 类型 | 数量 | 含义 |
| --- | --- | --- |
| 死类名（CSS 定义了但无人用） | **198** | `components.css` 201 个里 103 个（51%） |
| 不可静态判定（模板串拼装） | **33** | 如 `` `icon-tile-${tone}` `` |
| 幽灵类名（markup 用了但无定义） | **18** | 样式全靠同行内联 `style` 撑着 |
| 同名冲突（≥2 个样式表定义） | **51** | 胜负看 `styles/index.css` 的 `@import` 顺序 |

### 2.3 关键结论三条

1. **`components.css` 不是组件样式真源。** `src/styles/index.css` 第 9 行加载 `components.css`、第 10 行加载 `pages.css`，同名者后者胜——**46 个类名被 `pages.css` 覆盖**。所以它是一份"被部分覆盖的旧稿"。
2. **组件几乎没有自己的样式词汇表。** 12 个组件共用到 39 个类名，其中 **29 个与页面共享**，仅 10 个独占（`empty-state` `kpi-asset` `pagination` `pagination-info` `pagination-pages` `page-btn` `pagination-size` `pagination-jump` `tree-owned` `tree-denied`）。
3. **组件反向依赖宿主。** 见 2.4，这是本次最重要的新发现。

### 2.4 组件依赖拓扑（决定搬迁顺序）

| 组件 | 被谁引用 | 次数 | 内部依赖 | 搬运难度 |
| --- | --- | --- | --- | --- |
| `OverflowTooltip` | 5 个表单页 + `RoleCard` + `RoleAuthDropdown` | 6 | **ant-design-vue `Tooltip`** | 中（要定 antd 关系） |
| `StatusTag` | `Approval` `Home` `Expiring` `Permissions` | 4 | **宿主 `shared.ts`** | 低（同步契约） |
| `Icon` | `Shell` `RoleCard` | 2 | **宿主 `icons.ts`（62 行 SVG）** | 低（整体搬） |
| `SearchBox` | `Apply` `Home` | 2 | 无 | **零阻力** |
| `EmptyState` | `Expiring` `Permissions` | 2 | 无（内联 SVG） | **零阻力** |
| `PermissionTree` | `Expiring` `Permissions` | 2 | `PermissionTreeRows`、**宿主 `shared.ts`**、越界用 `.btn/.btn-ghost/.btn-sm/.perm-tree/.tree-*` | 高 |
| `Pagination` | `Home` | 1 | 越界用 `.select` `.input` `.active` | 中 |
| `KpiImg` | `Home` | 1 | **宿主 `shared.ts` + 宿主 `assets/kpi-*.png`（4 张）** | 中（要搬资产） |
| `Modal` | `Home` | 1 | 无 | **零阻力** |
| `PermissionTreeRows` | 仅 `PermissionTree` | 1 | **宿主 `shared.ts`（TreeNode 类型）** | 中 |
| `IconTile` | **0（孤儿）** | 0 | `Icon` | 待裁决 |
| `UrgencyTag` | **0（孤儿）** | 0 | **宿主 `shared.ts`** | 待裁决 |

**四条必须先处理的反向依赖：**

| 反向依赖 | 影响组件 | 处理方式 |
| --- | --- | --- |
| 宿主 `src/shared.ts` | StatusTag, UrgencyTag, KpiImg, PermissionTree, PermissionTreeRows | 拆分为「库内基础模块」+「宿主业务映射」两层（见 P1-T3） |
| 宿主 `src/icons.ts` | Icon | 整体搬入库（`src/foundation/icons.ts`） |
| 宿主 `src/assets/kpi-*.png` | KpiImg | 4 张图随组件搬入库 `src/assets/` |
| `ant-design-vue` | OverflowTooltip | 定为 **peerDependency**（宿主已装），或改为自研浮层（见 P0-T5 决策） |

### 2.5 页面层的可复用组件（P3 的候选）

`src/pages` 里其实藏着 10 个具复用价值的组件，只是位置放错了：

| 组件 | 位置 | 被引用 | 备注 |
| --- | --- | --- | --- |
| `InlineSearchSelect` | `pages/` 与 `pages/role-form/` **各一份** | 8（拆成 3 + 5） | **两份字节级完全相同（181 行）** |
| `ExpandedRowDetails` | `pages/role-form/` | 3 | 547 行 |
| `FormSelect` | `pages/role-form/` | 2 | 仅 29 行 |
| `RolePicker` | `pages/role-form/` | 1 | 263 行 |
| `RoleSingleSelect` | `pages/role-form/` | 1 | 110 行 |
| `RoleCard` | `pages/` | 1 | 67 行 |
| `PostLabelDatePicker` | `pages/` | 1 | 175 行 |
| `CreatePostLabelSection` | `pages/` | 1 | 203 行 |
| `AuthPermCell` / `AuthPermTreeParts` | `pages/` | 1 / 1 | 审批授权专用 |
| `RolePermTreeRow` | `pages/` | 1 | 审批悬浮卡专用 |
| `PortalMenu` / `RoleAuthDropdown` | `pages/role-form/` | **0 / 0** | 孤儿，待裁决 |

---

## 3. 目标架构

### 3.1 目录结构

```
D:\demo\组件库\                        ← 库（独立项目，可单开、可增删改）
├── package.json                        name: @yd/ui
├── tsconfig.json
├── src/
│   ├── index.ts                        ← 统一出口（唯一公开入口）
│   ├── foundation/                     ← 库自己的地基，不依赖任何宿主代码
│   │   ├── icons.ts                    ← 由宿主 src/icons.ts 迁入
│   │   ├── tag-class.ts                ← statusTagClass / urgencyTagClass + 映射表
│   │   ├── tree.ts                     ← TreeNode 类型 + countTreeNodes
│   │   └── dom.ts                      ← 通用 DOM 工具
│   ├── theme/
│   │   └── tokens.ts                   ← 唯一令牌真源（由宿主 src/theme/tokens.ts 迁入）
│   ├── assets/
│   │   └── kpi-*.png                   ← KpiImg 的 4 张图
│   ├── styles/
│   │   ├── index.css                   ← 库样式总入口
│   │   ├── tokens.css                  ← 生成物（勿手改）
│   │   ├── base.css                    ← 基础层（reset / 排版 / 焦点环）
│   │   └── components.css              ← 控件配方（仅控件语义类）
│   ├── components/                     ← 通用组件（12 个 → 收敛后）
│   │   ├── Icon.vue  IconTile.vue
│   │   ├── StatusTag.vue  UrgencyTag.vue
│   │   ├── Modal.vue  Pagination.vue  SearchBox.vue
│   │   ├── EmptyState.vue  KpiImg.vue  OverflowTooltip.vue
│   │   ├── PermissionTree.vue  PermissionTreeRows.vue
│   │   └── form/                       ← P3 新建
│   │       ├── FormField.vue  FormActions.vue  TableCol.ts
│   │       └── Selects/（基础 Select，收敛 6 种实现）
│   └── business/                       ← 业务组件（与权限域绑定）
│       ├── UserPicker.vue  UserChip.vue
│       ├── SystemPicker.vue
│       └── role/（RoleSection.vue 等）
├── scripts/
│   ├── gen-tokens.mjs                  ← tokens.ts → tokens.css（已存在，随迁）
│   ├── gen-spec.mjs                    ← tokens.ts → 设计规范文档 + showcase
│   ├── check-deps.mjs                  ← 依赖方向守卫
│   ├── check-docs.mjs                  ← 组件必须有规范条目
│   └── snapshot.cjs                    ← 视觉/计算样式快照与比对
└── docs/
    ├── 设计规范.md                      ← 生成物
    ├── tokens.json                     ← 生成物（机器可读令牌表）
    ├── showcase.html                   ← 生成物（离线单文件全状态渲染页）
    ├── 组件契约.md                      ← props / 事件 / 插槽 / 令牌消费清单
    └── 命名与归属规范.md
```

宿主侧新增/保留：

```
vue代码包/
├── src/App.vue                          ← antd 主题桥（宿主运行时合同，留宿主）
├── src/pages/                           ← 32 个业务页面（不进库）
├── src/styles/demo1/
│   ├── layout.css  pages.css  animations.css   ← 留宿主（页面语义）
│   └── (base / components / variables 移入库)
└── src/theme/tokens.ts                  ← 改为 re-export 库的 tokens（过渡期兼容）
```

### 3.2 依赖方向（单向，不可逆）

```
宿主 pages / Shell ──→ 库 @yd/ui ──→ foundation + theme + styles
                          │
                          └──→ peerDeps: vue, ant-design-vue
```

**三条禁令：**
1. 库内不出现指向宿主的相对路径（`../pages`、`../../shared`）或 `@/` 别名。
2. 库不 import 宿主的 `src/styles/*`。
3. 宿主不 import 库的内部路径（`@yd/ui/src/...`），只用公开出口。

### 3.3 运行时合同（必须写进文档，否则引用方少配一项就静默降级）

| 责任 | 归属 | 具体 |
| --- | --- | --- |
| 令牌定义 | 库 | `theme/tokens.ts` → 生成 `styles/tokens.css` |
| 样式加载 | 宿主 | 在 `styles/index.css` 顶部按序 `@import` 库的三层 CSS |
| 主题切换 | 宿主 | 在 `<html>` 上写 `data-theme="dark"`，库的 CSS 只认这个属性 |
| antd 主题桥 | 宿主 | `App.vue` 的 `ConfigProvider :theme`，从库的 tokens 取真值 |
| 暗色下的派生色 | 库提供真值 | 宿主负责 `algorithm: darkAlgorithm` 切换 |

> 这个合同本项目已经踩过坑：`App.vue` 的 antd 桥原先只搭了一半——`theme` 是静态对象、没有 `algorithm`，暗色下 antd 永远用亮色派生结果。库化之后同一类问题会以"引用方少配"的形式重现，所以合同要写进 `docs/组件契约.md`。

### 3.4 关键决策：库放在哪里，怎么被引用

| | 方案 A：独立目录 + 源码直引（**推荐首期**） | 方案 B：仓内 `packages/ui` + pnpm workspace |
| --- | --- | --- |
| 库位置 | `D:\demo\组件库`（已建） | `vue代码包\packages\ui` |
| 引用方式 | Vite alias + tsconfig paths 指向库 `src` | workspace 依赖 `@yd/ui: workspace:*` |
| 增删改生效 | 即时（HMR 直通源码，零构建零发版） | 即时 |
| 需要"同步"吗 | **不需要**——宿主读的就是库源码 | 不需要 |
| 跨盘限制 | 需配 `server.fs.allow` | 无 |
| 独立发给别的项目 | 可先给 alias 配置；要真包需补构建 | 需补构建或发 registry |
| 缺点 | 库不在宿主仓库树内，单独交付宿主则缺库 | "单独拉出来"程度低；改 workspace 根 |

**结论：首期用 A。** 理由：它已经满足了"单独引用 + 可增删改 + 改完同步"的全部诉求，且零构建成本、零版本管理。**切换时机**：当要交给别的项目复用、或要脱离本机交给别人跑时，改两处配置升级到 B（库内代码不动）。

---

## 4. 阶段总览

| 阶段 | 目标 | 前置 | 主要产出 | 复杂度 | 验证门 |
| --- | --- | --- | --- | --- | --- |
| **P0** 准备与裁决 | 定下所有不可由脚本决定的事 | — | 裁决表定稿、基线快照 | M | 每条裁决有依据，无悬空项 |
| **P1** 库骨架与地基 | 库能跑起来、宿主能引到 | P0 | 库工程 + foundation + tokens 迁移 | M | 宿主构建通过、样式零变化 |
| **P2** 搬迁 12 个既有组件 | 组件从宿主体内搬到库里 | P1 | 12 个组件 + 引用改写 | L | 亮暗快照逐项一致 |
| **P3** 消重复、造缺失组件 | 把 288 处重复变成组件 | P2 | UserPicker / UserChip / RoleSection 等 | L | 逐页替换后快照一致 |
| **P4** 设计规范文件 | 规范与代码同源 | P1（可与 P2/P3 并行） | 规范文档 + showcase + 契约表 | M | 规范数值全部来自生成 |
| **P5** 加锁与检查 | 防止再退化 | P2/P3 | 5 项检查 + CI 脚本 | S | 故意违规能被拦下 |
| **P6**（可选）可发布化 | 真包，给别处用 | P5 | lib 构建 + 类型声明 + exports | M | 外部项目能装能用 |

依赖关系：`P0 → P1 → P2 → P3 → P5`；`P4` 只依赖 `P1`，可与 P2/P3 并行。

---

## 5. 各阶段详细任务书

### P0 · 准备与裁决（不写业务代码）

目标：把脚本判不了的事定下来。**此阶段不产出任何 UI 改动，因此零回归风险。**

| 编号 | 任务 | 输入 | 产出 |
| --- | --- | --- | --- |
| P0-T1 | 建立回归基线 | 本地 dev server（5173） | 5 页 × 3 视口 × 亮/暗的计算样式 JSON + 截图，存 `.workbuddy/verify/baseline/` |
| P0-T2 | 裁决 51 条同名冲突 | `docs/类名归属裁决表.md` C 组 | 逐条标注归属（库/宿主/删除） |
| P0-T3 | 确认 33 条不可判定类 | 探测脚本 B 组 | 补 `EXTRA_DYNAMIC` 枚举值 或 标"已废弃" |
| P0-T4 | 处置 198 条死类 | 探测脚本 A 组 | 删除清单（先确认无动态引用） |
| P0-T5 | 裁决 4 个孤儿组件 | `IconTile` `UrgencyTag` `PortalMenu` `RoleAuthDropdown` | 启用 / 删除 |
| P0-T6 | 裁决 antd 与库的关系 | `OverflowTooltip` 依赖 antd `Tooltip` | 定 peerDependency 还是自研浮层 |
| P0-T7 | 裁决库的落地位置与引用方式 | 本文 3.4 | 确认 A 案并写入 `docs/组件契约.md` |

#### P0-T2 裁决规则（关键，按族裁决而非按类名）

**优先级：用量证据 > 语义猜测。** 但必须**同一控件的变体整族处理**，否则会得到"库里有 `.btn` 却没有 `.btn-primary`"的残缺。

先按族合并，再判族归属：

| 族 | 成员 | 建议归属 | 理由 |
| --- | --- | --- | --- |
| 按钮族 | `btn` `btn-primary` `btn-secondary` `btn-ghost` `btn-danger` `btn-sm` | **库** | 控件语义，页面与组件都在用 |
| 标签族 | `tag` `tag-*`（13 种状态） | **库** | 与 `StatusTag` 绑定 |
| 卡片族 | `card` `card-header` `card-title` `card-body` | **库** | 通用容器 |
| 表格族 | `table` `table-wrap` `cell-name` | **库** | 通用容器（但 `table-wrap` 的滚动渐隐层需一并搬） |
| 树族 | `perm-tree` `tree-*` `leaf` | **库** | 与 `PermissionTree` 绑定 |
| 页签族 | `tabs` `tab` `active` `selected` | `tabs`/`tab` 归库；`active`/`selected` **需细化** | 见下 |
| 布局族 | `col-panel*` `list-item*` `full` `gap-*` | **宿主** | 页面语义 |
| 表单控件族 | `input` `select` `textarea` `form-field` | **库** | 控件语义 |
| 反馈族 | `toast-root` `error-summary` | **库** | 通用反馈 |

`active` / `selected` 这两个是**状态修饰类**，不是组件：裁决方式是把它们**加前缀化**（`btn.active` → 由按钮族定义，`page-btn.active` → 由分页组件定义），而不是判给某一方。这一步需要在 P2 搬迁时同步改 CSS 选择器。

**验证门**：裁决表的每一行都能写清"谁在用、归于何处、改动后谁需要跟着改"，无悬空项。

**回滚**：无（不改代码）。

---

### P1 · 库骨架与地基

目标：库能跑、宿主能引，且**宿主界面一个像素不变**。

| 编号 | 任务 | 关键点 |
| --- | --- | --- |
| P1-T1 | 建库工程 | `package.json`（`name: @yd/ui`，`vue`/`ant-design-vue` 为 peerDeps）、`tsconfig.json` |
| P1-T2 | 宿主接入 | `vite.config.ts` 加 alias 与 `server.fs.allow`；`tsconfig.json` 加 paths |
| P1-T3 | 拆 `shared.ts` | 见下（**本阶段最需要判断力的一步**） |
| P1-T4 | 迁 `icons.ts` | 整体搬入库 `foundation/icons.ts`，宿主改为 re-export（过渡） |
| P1-T5 | 迁 `tokens.ts` | 搬入库 `theme/tokens.ts`；宿主 `src/theme/tokens.ts` 改 re-export；`gen-tokens.mjs` 随迁并改输出路径 |
| P1-T6 | 样式分层 | 拆出库的三层 CSS，宿主 `styles/index.css` 改为按序 import |
| P1-T7 | 公开出口 | 库 `src/index.ts` 统一导出；宿主改 import 来源 |

#### P1-T3 拆 `shared.ts`（反向依赖的根）

宿主的 `shared.ts` 混了两类东西，必须分开：

| 现有成员 | 性质 | 去向 |
| --- | --- | --- |
| `STATUS_TAG_CLASS` / `statusTagClass` | 控件语义（标签状态映射） | **入库** `foundation/tag-class.ts` |
| `urgencyTagClass` | 控件语义 | **入库** |
| `TreeNode` / `countTreeNodes` | 通用类型与工具 | **入库** `foundation/tree.ts` |
| `riskLevel(days)` | **业务规则**（3 天/7 天阈值） | **留宿主**，但返回值里的 `cls` 由库提供 |
| `KPI_ASSETS` | 资产映射 | **入库**，图片一并搬 |

拆分后宿主 `shared.ts` 只保留 `riskLevel` 这类业务规则，并 `import { tagClassOf } from '@yd/ui'`。

#### P1-T6 样式分层与加载顺序（最容易出错的一步）

现状（`src/styles/index.css`）：

```
1 fonts  2 tailwind  3 theme.css
6 demo1/variables  7 base  8 layout  9 components  10 pages  11 animations
```

拆分后宿主侧：

```css
/* 宿主 src/styles/index.css */
@import '@yd/ui/styles/tokens.css';      /* 库：令牌（原 demo1/variables.css） */
@import '@yd/ui/styles/base.css';        /* 库：基础层（原 demo1/base.css 的控件无关部分） */
@import '@yd/ui/styles/components.css';  /* 库：控件配方（原 demo1/components.css，去掉死类与页面类） */
@import './fonts.css';
@import './tailwind.css';
@import './theme.css';
@import './demo1/layout.css';            /* 宿主：布局 */
@import './demo1/pages.css';             /* 宿主：页面（已清掉控件类） */
@import './demo1/animations.css';
```

**顺序不变式：库的控件配方必须仍在 `pages.css` 之前。** 原来那 46 处 `components.css` → `pages.css` 的覆盖关系，正是在 P0-T2 裁决掉的那些；若顺序反了，会静默出现外观变化。

**已知风险与兜底**：Vite 对 CSS `@import` 的 alias 解析在少数版本上不稳定。若报解析失败，改为在 `src/main.ts` 里按顺序显式 import 各 CSS 入口文件（同等效果，顺序由 JS 控制）。

**验证门**：
- 宿主 `vite build` 通过；
- P0-T1 的基线快照**逐项一致**（这是本阶段唯一的硬门）；
- 改库内一个颜色值，宿主页面 3 秒内变化（证明直引生效）。

**回滚**：库目录可整目录弃用；宿主侧只动了 3 个配置文件与 1 个 CSS 顺序表，反向改回即可。迁移前备份 `variables.css`、`components.css`、`base.css`。

---

### P2 · 搬迁 12 个既有组件

目标：组件从宿主体内搬到库里，**引用方代码与视觉都不变**。

按依赖阻力分三批，每批独立验收：

#### 批次 1：零阻力（可立即做，风险最低）

| 组件 | 引用方改写 | 需同步搬走的 CSS |
| --- | --- | --- |
| `SearchBox` | `Apply` `Home` | `.search-box` `.search-icon` `.input`（`.input` 属控件族，P0 裁决后归库） |
| `EmptyState` | `Expiring` `Permissions` | `.empty-state` |
| `Modal` | `Home` | `.modal-backdrop` `.modal` `.modal-{sm,lg,xl}` `.modal-header/-title/-close/-body/-footer` |
| `Icon` + `IconTile` | `Shell` `RoleCard`（`IconTile` 无引用，一并搬） | `.icon-tile` `.icon-tile-{tone}` `.icon-tile-{sm,lg}` |

#### 批次 2：需要解依赖

| 组件 | 要解决的依赖 |
| --- | --- |
| `StatusTag` `UrgencyTag` | 换 import 来源为库 `foundation/tag-class.ts`；CSS：`.tag` + 13 个 `.tag-*` + `.tag-urgent/-high/-normal` |
| `KpiImg` | 4 张 `kpi-*.png` 搬入库 `assets/`；`.kpi-asset` |
| `PermissionTreeRows` + `PermissionTree` | 换 `TreeNode` 来源；`.perm-tree` `.tree-toolbar` `.tree-toolbar-btn` `.tree-list` `.tree-row` `.tree-level-*` `.tree-toggle` `.leaf` `.tree-name` `.tree-type` `.tree-children` `.tree-owned` `.tree-denied` + 依赖的 `.btn/.btn-ghost/.btn-sm` |
| `Pagination` | `.pagination` `.pagination-info/-pages/-size/-jump` `.page-btn` `.active`（需前缀化） |

#### 批次 3：需要合同决策

| 组件 | 决策项 |
| --- | --- |
| `OverflowTooltip` | 沿用 antd `Tooltip`（定 peerDependency）还是改自研浮层（对齐"去 antd"终局）。**建议首期沿用**，因为它已用 rAF 合并与 ResizeObserver 做了溢出测量，逻辑本身没问题，改浮层是另一件事 |

#### 孤儿组件处置（P0-T5 的执行）

| 组件 | 建议 | 理由 |
| --- | --- | --- |
| `IconTile` | **启用** | `Apply.vue` 手写 `` `flow-card-icon icon-tile icon-tile-${TONES[...]}` `` 绕过了它——组件是对的，是页面没采用 |
| `UrgencyTag` | **启用** | 各页面手写 `` `tag ${urgencyTagClass(...)}` ``，同样是绕过 |
| `PortalMenu` | 确认后删除或搬入库 | 入度 0，82 行 |
| `RoleAuthDropdown` | 确认后删除或搬入库 | 入度 0，391 行（且依赖 `OverflowTooltip`） |

> 启用两个孤儿组件本身就能消除一批手写实现，是 P3 之前的免费收益。

**验证门（每批次独立跑）**：
1. 亮/暗双主题 × 桌面/平板/移动三视口的计算样式快照与基线一致；
2. 该批次受影响页面的截图逐像素比对无差异；
3. `vite build` 通过。

**回滚**：每批次单独一次提交；回滚即还原该批次的组件目录与引用行。

---

### P3 · 消重复、造缺失组件

目标：**重复处已经替你标出了组件边界**——288 处完全重复的内联样式，每一组对应一个尚未被抽象出来的组件。

按"改动集中度 / 收益"排序：

| 顺序 | 重复组 | 次数 | 抽成 | 涉及文件 | 收益 |
| --- | --- | --- | --- | --- | --- |
| 1 | `user-picker-chip`（含 6 项内联样式的一行 div） | ×7 | `UserChip` | 7 个表单页 | 消 42 处内联样式 |
| 2 | 用户选择弹窗区块（约 70 行 markup + 内联样式） | ×7 | `UserPicker` | 7 个表单页 | 消约 490 行 markup 重复 |
| 3 | `system-pick-item` 系统选择项 | ×6 | `SystemPicker` | 5 个表单页 + `IDaaSRoleForm` | 消 36 处内联样式 |
| 4 | `{ width:'100%', minHeight:'32px' }` | ×20 | `FormField` / 令牌 | 4 个文件 | 消 20 处内联样式 |
| 5 | `Create/Modify/DeleteRoleSection` 三个近亲文件 | ×3 | 合并为 `RoleSection` + `mode` prop | `role-form/` | 消约 600 行重复 |
| 6 | 表格列宽内联样式（120px/160px/44px/56px/80px） | ×9/×5/×4/×3/×3 | 列宽令牌 或 `TableCol` | `role-form/` ×3 | 消 24 处内联样式 |
| 7 | `InlineSearchSelect` 两份字节级重复 | ×2 | 只留库内一份 | `pages/` `pages/role-form/` | 消 181 行重复 |
| 8 | 6 种下拉实现 + 4 个原生 `<select>` | — | 收敛为 1 个基础 `Select` | 全项目 | 统一交互与外观 |

**顺序理由**：1-3 是"纯复制粘贴"，替换是机械操作，收益最大、风险最低；5、7、8 涉及行为统一，必须放后。

**每步的动作规范**：
1. 先在库内写组件（含 props 契约与默认值），**不改任何页面**；
2. 建立规范条目（P4 的产物同步更新）；
3. 只替换**一个**页面，跑快照比对；
4. 一致后再替换其余页面，每页一次提交；
5. 全部替换完，删除页面的旧实现与已无引用的内联样式。

**验证门**：每替换一页跑一次快照；全部完成后重跑 `coupling-probe`，确认：
- 内联 `style` 从 519 降到合理量（目标 ≤150）；
- 完全重复组从 77 组降到 ≤15 组；
- 幽灵类名从 18 降到 0（新组件把占位类名变成真实类名）。

**回滚**：按页提交，可逐页还原。

---

### P4 · 设计规范文件（与代码同源生成）

目标：规范文件是**生成物**，不是手写文档。手写的规范三周后必然漂移——这正是 `App.vue` 里 antd 令牌抄了第二份 hex 的成因。

| 编号 | 产出 | 内容 | 生成方式 |
| --- | --- | --- | --- |
| P4-T1 | `docs/tokens.json` | 亮/暗全量令牌，机器可读 | `gen-spec.mjs` 读 `tokens.ts` |
| P4-T2 | `docs/设计规范.md` | 令牌表、层级阶梯（字号/字重/行高）、间距阶梯、圆角/阴影/动效时长、亮暗对比度实测值 | 同上 |
| P4-T3 | `docs/showcase.html` | **离线单文件**全状态渲染页：每个组件的默认/hover/active/disabled/加载/空态/暗色 | 生成静态 HTML（无外链、无登录、可离线打开） |
| P4-T4 | `docs/组件契约.md` | 每个组件的 props / 事件 / 插槽 / 消费的令牌 / 依赖的类名 | 半自动：脚本抽取 props，人工补语义 |
| P4-T5 | `docs/命名与归属规范.md` | 类名前缀规则、令牌命名规则、组件命名规则、什么该进库/什么留宿主 | 人工撰写，脚本检查落地 |
| P4-T6 | `scripts/gen-spec.mjs` | 生成器；`--check` 模式用于 CI | — |

**规范必须回答的四个问题**（否则前端引用时会各自猜）：
1. 这个组件消费哪些令牌？（改造时改哪个变量）
2. 它的所有状态长什么样？（showcase 给出可点开的样子）
3. 命名边界在哪？（什么算控件、什么算页面）
4. 引用它需要宿主配什么？（3.3 的运行时合同）

**验证门**：改 `tokens.ts` 里一个颜色 → 规范文档与 showcase 的数值同步变化；`npm run check:spec` 在未同步时退出码为 1。

---

### P5 · 加锁与检查

| 编号 | 检查 | 拦什么 | 实现 |
| --- | --- | --- | --- |
| P5-T1 | `check:tokens` | 令牌 CSS 与 `tokens.ts` 不同步 | 已存在（`gen-tokens.mjs --check`） |
| P5-T2 | `check:deps` | 库反向 import 宿主 | 静态扫描库内 import 语句 |
| P5-T3 | `check:classes` | CSS 定义了但无人用 / markup 用了但无定义 | 复用 `coupling-probe.cjs`，把两类差异设为非零退出 |
| P5-T4 | `check:docs` | 新增组件没有规范条目 | 比对 `src/components/**` 与 `docs/组件契约.md` |
| P5-T5 | `check:spec` | 规范文件与代码不同步 | `gen-spec.mjs --check` |
| P5-T6 | `stylelint` | 裸 hex 色值、`!important` | 配置规则，令牌定义文件加白名单例外 |
| P5-T7 | `npm run check` | 一键跑全部 | 串联上述 6 项 |

**必须先解决的现实约束**：本机 `pnpm`/`npm` 的包装脚本在沙箱下路径解析失败（会把自己解析成 `d:\node_modules\pnpm`），且缺 `sed`/`dirname` 等命令。安装 stylelint 等依赖时必须**绕过包装层，用 node 直调 pnpm 的 `.mjs` 入口**：

```
node <node.exe> <.../node_modules/pnpm/bin/pnpm.mjs> add -D stylelint ...
```

**验证门**：人工制造 3 类违规（库内 import 宿主、裸 hex、无文档的新组件），`npm run check` 全部拦下。

---

### P6 ·（可选）可发布化

当库要给宿主之外的项目用时才做：

- 补库构建（Vite library mode），产出 ESM + 类型声明（`.d.ts`）；
- 写 `exports` map：`.` / `./styles` / `./theme`；
- `vue` 与 `ant-design-vue` 保持 peerDependency 并 external；
- **不要**在契约稳定前发到私有 registry——会立刻陷入"改一处 → 发版 → 升依赖"的循环。

---

## 6. 同步机制（"改完库同步回 vue 代码"怎么实现）

| 机制 | 是否需要手动同步 | 生效延迟 | 首期是否采用 |
| --- | --- | --- | --- |
| **Vite alias 直引库源码** | **不需要** | 即时（HMR） | **采用** |
| pnpm workspace 依赖 | 不需要 | 即时 | P6 备选 |
| `file:` 依赖（symlink） | 不需要 | 即时 | — |
| 构建产物 + 复制脚本 | 需要（跑脚本） | 手动 | **不采用** |
| git submodule | 需要（拉取更新） | 手动 | 不采用 |

**机制说明**：宿主 Vite 的 alias 直接指向 `D:\demo\组件库\src`，因此
- **增**：库内新建组件 → 库 `index.ts` 加一行导出 → 宿主 import 即可用；
- **删**：库内删除组件 → 宿主若仍 import 会立刻编译报错（这正是想要的效果，比静默失效好）；
- **改**：库内改组件 → 宿主页面 HMR 即时更新，无需重启、无需发版。

**反向流动（宿主特例不下沉）**：若某个改动只服务某一个页面，它应该留在宿主 `pages/`，不进库。判据：**该 UI 是否在 ≥2 个页面出现，且语义与权限域无关。** 不满足就留宿主。这条要写进 P4-T5 的规范文档，否则库会被业务特例污染。

---

## 7. 验证与回归体系

项目无单测、无视觉基线，所以回归只能靠实测。三层：

| 层 | 手段 | 抓什么 | 工具 |
| --- | --- | --- | --- |
| 结构层 | 静态解析 | 死类/幽灵类/同名冲突/内联重复/依赖越界 | `coupling-probe.cjs`（已存在，无依赖） |
| 表现层 | 浏览器实测计算样式 | 颜色/对比度/尺寸/溢出/触达尺寸 | `~/.workbuddy/skills/frontend-measure-audit/scripts/measure.cjs` |
| 观感层 | 截图逐像素比对 | 表现层漏掉的视觉差异 | Playwright + 系统 Chrome |

**基线口径**：`5 个导航页 × 3 视口 × 亮/暗 + 7 个流程表单页 + 审批详情`，与体检报告保持一致，便于数据复用。

**环境要点（本机已验证）**：`playwright-core` 位于 managed node workspace，配系统 Chrome；脚本用 `.cjs`；需设 `NODE_PATH`。

---

## 8. 风险登记册

| # | 风险 | 触发条件 | 缓解 | 严重度 |
| --- | --- | --- | --- | --- |
| R1 | 双份并存期出现"新旧实现同时存在" | P2/P3 迁移中 | 逐批次、逐页切换，不一把切；每批次独立提交 | 高 |
| R2 | 搬迁时顺手改视觉，导致无法判断回归来源 | 想"反正要改" | 硬约束：**搬迁阶段目标是像素级不变**，视觉调整另开一轮 | 高 |
| R3 | CSS 加载顺序变化导致静默外观漂移 | P1-T6 | 保持"库控件配方在 `pages.css` 之前"的不变式；先跑快照再合 | 高 |
| R4 | 库被业务特例污染，复用性下降 | P3 无节制下沉 | 判据写进规范：≥2 页面 + 语义无关才进库 | 中 |
| R5 | 引用方少配运行时合同导致静默降级 | 库被别的项目引用 | 合同写进 `docs/组件契约.md`；提供一行式的样式入口 | 中 |
| R6 | 裁决不准导致外观变化 | P0-T2 | 每族裁决后先在单个页面验证 | 中 |
| R7 | 本机包管理器损坏导致依赖装不上 | P5 装 stylelint | 用 node 直调 `pnpm.mjs`；不能在沙箱内 `npm install` 时就跳过该项并记录 | 低 |
| R8 | 库在宿主仓库树外，单独交付宿主时缺库 | 移交他人 | 交付时说明；或按 P6 升级为 workspace | 低 |

---

## 9. 不建议做的事

- **不要一开始就发独立 npm 包。** 契约未稳，版本管理成本会立刻超过收益。
- **不要整文件搬运 CSS。** 会带进 198 个死类里的一半，并让 46 处覆盖关系失去仲裁。
- **不要在搬迁的同时改视觉。** 无法归因回归。
- **不要指望脚本裁决全部 51 条冲突。** 那 51 条正是需要设计判断的部分。
- **不要手写设计规范。** 必然漂移，本项目已经因此吃过一次亏。
- **不要为了"统一"而把页面私有 UI 也抽成组件。** 只服务一个页面的东西，留在页面里才是对的。
- **不要逐条打补丁修对比度。** 约 325 处不达标的根因只是一个 `--text-muted` 值，改源头。

---

## 10. 立即可执行的第一步

P0 无需任何环境准备，可以现在就做：

```
1. 启动本地预览（若未运行）：http://localhost:5173/
2. 采集基线：跑 measure.cjs 与截图脚本，落到 .workbuddy/verify/baseline/
3. 打开 docs/类名归属裁决表.md，按 P0-T2 的"族"规则逐族填归属
4. 输出《P0 裁决结论》——51 条冲突逐条落定 + 4 个孤儿组件去留 + antd 关系决策
```

第 3 步是整条路线的真正闸门：**裁决不定，P1 之后的每一步都可能在改样式时踩到"到底以哪个文件的定义为准"。**

---

## 附录 A · 关键配置片段

**宿主 `vite.config.ts` 增补**

```ts
import path from 'path'

resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
    '@yd/ui': 'D:/demo/组件库/src',
  },
},
server: {
  fs: {
    // 库在宿主工作区之外，需显式放行
    allow: [path.resolve(__dirname), 'D:/demo/组件库'],
  },
},
```

**宿主 `tsconfig.json` 增补**

```json
"paths": {
  "@/*": ["./src/*"],
  "@yd/ui": ["../组件库/src/index.ts"]
}
```

**库 `package.json`（首期，源码直引，无构建）**

```json
{
  "name": "@yd/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./styles": "./src/styles/index.css",
    "./theme": "./src/theme/tokens.ts"
  },
  "peerDependencies": {
    "vue": "^3.4.0",
    "ant-design-vue": "^4.0.0"
  }
}
```

**库 `src/index.ts` 出口形态**

```ts
// 组件
export { default as Icon } from './components/Icon.vue'
export { default as Modal } from './components/Modal.vue'
// … 其余组件同理

// 地基
export * from './foundation/tag-class'
export * from './foundation/tree'

// 令牌（供宿主 App.vue 的 antd 桥使用）
export { lightTokens, darkTokens, light, dark } from './theme/tokens'
```

---

## 附录 B · 复核本文数字

所有静态数字由项目内脚本产生，可随时重跑：

```bash
# 耦合度探测（死类 / 幽灵类 / 同名冲突 / 内联重复 / 零引用组件）
node scripts/coupling-probe.cjs --root . --src src --out coupling.json

# 类名归属裁决表
node scripts/gen-class-ownership.cjs --json coupling.json --out docs/类名归属裁决表.md

# 令牌同步检查
node scripts/gen-tokens.mjs --check
```

两个已知判定边界（复核时会有 ±1~2 浮动）：
- 由模板串拼装的类名不可静态判定，被单独归入"不可判定"而非"死类"；把这些枚举值补进脚本后，死类数会少几个。
- 幽灵类名数量对解析方式敏感：`:class="…"` 里含子串 `class="`，用朴素正则会虚高到 90+；脚本已用否定后顾排除。
