{
  "package": "@yd/ui",
  "version": "0.1.0",
  "title": "Portal Design · 权限自助门户组件库",
  "updatedAt": "2026-09-22",
  "stage": "P3 进行中（从宿主表单/展示件抽取 6 个组件：PortalMenu / SearchSelect / SingleSelect / SelectCard / ChipTree / HoverCard；两层结构与门禁沿用 P2）",
  "layers": {
    "note": "库分两层，依赖方向严格单向：vue → core。core 是 vue 的超集 —— 用 vue 层能拿到的视觉，用 core 层的类名一样能拿到；反过来不成立。",
    "core": {
      "tier": 0,
      "title": "零框架层",
      "entry": "@yd/ui/core",
      "path": "src/core/",
      "desc": "不依赖 vue / ant-design-vue，可被原生 HTML、静态页、任意框架或后端模板直接使用。",
      "contains": [
        "styles/ 样式三层（tokens → base → components）",
        "icons/ 41 个 SVG 符号（纯字符串表）",
        "assets/ KPI 配图（4 张 PNG，随库发布）",
        "contract/ 控件语义映射（状态词表、树类型、素材表、皮肤钩子）",
        "theme/tokens.ts 设计令牌真源"
      ],
      "hardRules": [
        "C1 core 不得 import vue / ant-design-vue",
        "C2 core 里不得出现 .vue 文件，也不得 import .vue",
        "C3 core 不得反向依赖宿主或 vue 层（无 @/ 别名、相对路径不得越出库根）"
      ]
    },
    "vue": {
      "tier": 1,
      "title": "组件层",
      "entry": "@yd/ui/vue",
      "path": "src/vue/",
      "desc": "18 个 SFC。只依赖 core，被 core 依赖是违规。",
      "hardRules": [
        "V1 组件不得带 <style> 块 —— 视觉一律由 core/styles/components.css 的类名提供",
        "V2 模板输出的库类名必须在类名契约内（或已登记的皮肤钩子）"
      ]
    },
    "aggregate": "src/index.ts 同时导出两层，`import { Modal } from '@yd/ui'` 即可。只想用样式与类名时不要引这个出口 —— 引 @yd/ui/core 或直接用 dist-lite。"
  },
  "usage": {
    "styles": [
      "@import '@yd/ui/core/styles/tokens.css';",
      "@import '@yd/ui/core/styles/base.css';",
      "@import '@yd/ui/core/styles/components.css';"
    ],
    "stylesNote": "三条 @import 的位置就是级联顺序，必须都排在宿主页面样式（pages.css）之前。顺序反转会让页面上下文覆盖静默失效。",
    "import": "import { Modal, StatusTag, PermissionTree } from '@yd/ui'",
    "nativeUsage": {
      "desc": "原生 HTML / 零构建路径：不装 Vue、不跑构建，只引一个合并后的 CSS 与 sprite。",
      "css": "<link rel=\"stylesheet\" href=\"./dist-lite/yd-ui.css\">",
      "sprite": "<svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\"><use href=\"./dist-lite/icons.svg#yd-icon-shield\"/></svg>",
      "assets": "<img class=\"kpi-asset\" src=\"./dist-lite/assets/kpi-pending.png\" width=\"40\" height=\"40\" alt=\"\">",
      "manifest": "dist-lite/manifest.json —— CSS/sprite 哈希、图标 id 清单、素材文件名",
      "contract": "schema/class-contract.json —— 33 家族 / 190 类名，查类名用这一份",
      "note": "dist-lite 是提交进仓库的构建产物（原生消费者不跑构建）。改完 core/styles 必须重建：npm run build:lite；CI 用 npm run check:lite 守同步。"
    },
    "peerDependencies": ["vue ^3.4.0（仅 vue 层需要）", "ant-design-vue ^4.2.0（仅 OverflowTooltip 需要）"],
    "bans": [
      "R1 库不得 import 宿主任何模块（禁止反向依赖）",
      "R2 组件不得带 <style> 块，样式一律进 core/styles 的对应层",
      "R3 库不得写死业务规则（如到期风险阈值），业务规则留宿主",
      "R4 core 不得依赖 vue 层（依赖方向只有 vue → core）"
    ],
    "gates": {
      "check:layering": "scripts/check-layering.cjs —— 守 C1/C2/C3/V1/V2 + A1(三层顺序)/A2(皮肤钩子有效)",
      "check:lite": "scripts/build-lite.mjs --check —— 守 dist-lite 与 core 源码同步",
      "vanilla-parity": "scripts/vanilla-parity.cjs —— 真实浏览器逐属性比对「原生 HTML 渲染」与「Vue 组件渲染」是否一致",
      "vanilla-probe": "scripts/vanilla-probe.cjs —— 原生示例页验收：零报错、sprite 可解析、各族非零渲染、语义配色分组",
      "selftest": "scripts/check-layering.selftest.cjs —— 给门禁灌注违规，证明它真的会失败（14 项）"
    }
  },
  "groups": [
    { "key": "general", "title": "通用" },
    { "key": "form", "title": "表单" },
    { "key": "data", "title": "数据展示" },
    { "key": "feedback", "title": "反馈" },
    { "key": "navigation", "title": "导航" }
  ],
  "components": [
    {
      "name": "Icon",
      "title": "图标",
      "group": "general",
      "status": "stable",
      "desc": "内联 SVG 图标。图标集是纯字符串表（`core/icons/icons.ts`，零框架层），不依赖图标字体，因此可以整表被 AI 读取后按名引用。",
      "props": [
        { "name": "name", "type": "string", "required": true, "desc": "图标键名，取值见 icons 表（home / apply / approval / expiry / permissions / brand / bell / sun / moon / panelLeft / file / users / send / key / shield / clock / check / chevronRight / spark / navHome / navApply / navApproval / navExpiring / navPermissions / chevronDown / hamburger / collapse / sunOutline / moonOutline / bellOutline 及 *Fill 别名）" },
        { "name": "className", "type": "string", "desc": "透传到 <span> 的附加类名，用于控制尺寸与颜色" }
      ],
      "slots": [],
      "events": [],
      "a11y": ["根元素固定 aria-hidden=\"true\"（装饰性图标），语义文本需由使用方另行提供"],
      "demos": [
        {
          "title": "基础用法",
          "desc": "`name` 未命中时组件不渲染任何内容。",
          "code": "<Icon name=\"shield\" className=\"icon-18\" />\n<Icon name=\"key\" className=\"icon-18\" />\n<Icon name=\"users\" className=\"icon-18\" />"
        }
      ],
      "native": {
        "available": true,
        "code": "<svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\">\n  <use href=\"./dist-lite/icons.svg#yd-icon-shield\"/>\n</svg>",
        "note": "Vue 侧的 Icon 只是把真源表里的 SVG 字符串塞进一个 aria-hidden 的 <span>，没有任何样式逻辑。原生侧不用组件，改用 sprite：<use href=\"#yd-icon-{name}\">。描边色是 currentColor，颜色由外层文字色决定；尺寸由 width/height 决定。可用 id 见 dist-lite/manifest.json 的 icons。"
      }
    },
    {
      "name": "IconTile",
      "title": "图标方块",
      "group": "general",
      "status": "stable",
      "desc": "给图标套一个带底色圆角容器。常用于卡片头、统计块、导航项。",
      "props": [
        { "name": "name", "type": "string", "required": true, "desc": "图标键名，同 Icon" },
        { "name": "tone", "type": "'blue' | 'cyan' | 'purple' | 'orange' | 'green' | 'rose'", "default": "'blue'", "desc": "配色。类名为 `icon-tile-{tone}`，全部由库的样式层定义" },
        { "name": "size", "type": "'sm' | 'lg'", "desc": "尺寸。不传为默认 36px，sm=32px，lg=44px" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        {
          "title": "六种配色",
          "desc": "`tone` 取值空间由样式层的 `icon-tile-*` 唯一决定，组件不做校验。",
          "code": "<IconTile name=\"shield\" tone=\"blue\" />\n<IconTile name=\"key\" tone=\"cyan\" />\n<IconTile name=\"users\" tone=\"purple\" />\n<IconTile name=\"send\" tone=\"orange\" />\n<IconTile name=\"check\" tone=\"green\" />\n<IconTile name=\"file\" tone=\"rose\" />"
        },
        {
          "title": "三种尺寸",
          "code": "<IconTile name=\"shield\" size=\"sm\" />\n<IconTile name=\"shield\" />\n<IconTile name=\"shield\" size=\"lg\" />"
        }
      ],
      "native": {
        "available": true,
        "code": "<span class=\"icon-tile icon-tile-blue\">\n  <svg width=\"18\" height=\"18\" viewBox=\"0 0 24 24\">\n    <use href=\"./dist-lite/icons.svg#yd-icon-shield\"/>\n  </svg>\n</span>",
        "note": "tone → .icon-tile-{tone}，尺寸 → .icon-tile-sm / .icon-tile-lg（不传为默认 36px）。全部由 core/styles/components.css 定义，Vue 组件只负责拼类名。icon-3d-* 是同源历史别名。"
      }
    },
    {
      "name": "SearchBox",
      "title": "搜索框",
      "group": "general",
      "status": "stable",
      "desc": "带前置放大镜的搜索输入。目前是**纯展示控件**：只透传 placeholder，未绑定 v-model 或 search 事件。",
      "props": [
        { "name": "placeholder", "type": "string", "default": "'搜索'", "desc": "占位文案，同时用作 input 的 aria-label" }
      ],
      "slots": [],
      "events": [],
      "a11y": ["输入框用 `placeholder` 作为 aria-label，若页面另有可见 label 建议改为传 aria-label"],
      "demos": [
        { "title": "基础用法", "code": "<SearchBox />\n<SearchBox placeholder=\"搜索角色或应用\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"search-box\">\n  <span class=\"search-icon\" aria-hidden=\"true\">\n    <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.8\">\n      <circle cx=\"11\" cy=\"11\" r=\"7\"/><path d=\"M20 20l-3-3\" stroke-linecap=\"round\"/>\n    </svg>\n  </span>\n  <input type=\"search\" class=\"input\" placeholder=\"搜索\" aria-label=\"搜索\"/>\n</div>",
        "note": "结构固定为 .search-box > (.search-icon + .input)。注意放大镜图形**不在图标 sprite 里** —— 它是 SearchBox.vue 的私有常量，所以原生侧要自备这个 SVG；库只提供 .search-icon 的位置与配色。"
      }
    },
    {
      "name": "EmptyState",
      "title": "空状态",
      "group": "data",
      "status": "stable",
      "desc": "列表/表格无数据时的占位。图标是内置的固定 SVG，不接受替换。",
      "props": [
        { "name": "title", "type": "string", "default": "'暂无数据'", "desc": "主文案" },
        { "name": "desc", "type": "string", "default": "''", "desc": "辅助说明，为空时不渲染 <p>" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        { "title": "基础用法", "code": "<EmptyState />" },
        { "title": "带说明", "code": "<EmptyState title=\"没有待审批的申请\" desc=\"所有申请都已处理完毕。\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"empty-state\">\n  <svg viewBox=\"0 0 48 48\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\">\n    <rect x=\"8\" y=\"12\" width=\"32\" height=\"28\" rx=\"2\"/>\n    <path d=\"M16 12V10a8 8 0 0116 0v2M18 24h12M18 30h8\"/>\n  </svg>\n  <h3>暂无数据</h3>\n  <p>说明文字可省。</p>\n</div>",
        "note": "子元素顺序固定：svg → h3 → p（p 可省）。.empty-state 提供居中、间距与配色；图形本身是组件的私有常量、不在 sprite 里，原生侧需自备，但形状不属样式层，换图形不影响这套类名。"
      }
    },
    {
      "name": "StatusTag",
      "title": "状态标签",
      "group": "data",
      "status": "stable",
      "desc": "把业务状态词映射成标签配色。映射表 `STATUS_TAG_CLASS` 在库的零框架层（`core/contract/tag-class.ts`），是状态词取值空间的**唯一真源**；未命中的词回落 `tag-info`。原生 HTML 侧无需调用函数——直接查 `class-contract.json` 的 `statusWords` 表拿到类名。",
      "props": [
        { "name": "status", "type": "string", "required": true, "desc": "状态词。已登记：待处理 / 待审批 / 审批中 / 抄送我 / 已通过 / 已驳回 / 已撤回 / 已终止 / 延期审批中 / 有效 / 即将到期 / 已过期 / 正常" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        { "title": "审批状态", "code": "<StatusTag status=\"待审批\" />\n<StatusTag status=\"审批中\" />\n<StatusTag status=\"已通过\" />\n<StatusTag status=\"已驳回\" />\n<StatusTag status=\"已撤回\" />" },
        { "title": "权限状态", "code": "<StatusTag status=\"有效\" />\n<StatusTag status=\"即将到期\" />\n<StatusTag status=\"已过期\" />\n<StatusTag status=\"未登记的词\" />" }
      ],
      "native": {
        "available": true,
        "code": "<span class=\"tag tag-pending\">待审批</span>\n<span class=\"tag tag-approved\">已通过</span>\n<span class=\"tag tag-info\">未登记的词</span>",
        "note": "Vue 侧 StatusTag 做的就是「查表 → 拼类名」，没有任何视觉。原生侧直接查表：STATUS_TAG_CLASS 的真源是 core/contract/tag-class.ts，导出为 schema/class-contract.json 的 statusWords。未登记的词回落 tag-info。不要自己拼 tag- 前缀。"
      }
    },
    {
      "name": "UrgencyTag",
      "title": "紧急度标签",
      "group": "data",
      "status": "stable",
      "desc": "三档紧急度标签。与 StatusTag 共用 `.tag` 基础配方，只是配色映射表不同。",
      "props": [
        { "name": "level", "type": "string", "required": true, "desc": "紧急度词。已登记：非常紧急 / 紧急 / 普通；其余一律按「普通」渲染" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        { "title": "三档", "code": "<UrgencyTag level=\"非常紧急\" />\n<UrgencyTag level=\"紧急\" />\n<UrgencyTag level=\"普通\" />" }
      ],
      "native": {
        "available": true,
        "code": "<span class=\"tag tag-urgent\">非常紧急</span>\n<span class=\"tag tag-high\">紧急</span>\n<span class=\"tag tag-normal\">普通</span>",
        "note": "查 URGENCY_TAG_CLASS 表（core/contract/tag-class.ts）。未登记的紧急度一律按 tag-normal 渲染，不回落 tag-info —— 这一点与 StatusTag 不同。"
      }
    },
    {
      "name": "KpiImg",
      "title": "KPI 配图",
      "group": "data",
      "status": "stable",
      "desc": "统计卡片的配图。图片资源随库一起发布（`core/assets/`，零框架层），因此库自包含，不反向依赖宿主的 assets。键空间见 `core/contract/assets-manifest.ts`；原生 HTML 侧直接引 `dist-lite/assets/kpi-*.png`。",
      "props": [
        { "name": "kpiKey", "type": "'pending' | 'processed' | 'submitted' | 'cc'", "required": true, "desc": "语义键，映射见 `KPI_ASSETS`" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        { "title": "四种语义", "code": "<KpiImg kpiKey=\"pending\" />\n<KpiImg kpiKey=\"processed\" />\n<KpiImg kpiKey=\"submitted\" />\n<KpiImg kpiKey=\"cc\" />" }
      ],
      "native": {
        "available": true,
        "code": "<img class=\"kpi-asset\" src=\"./dist-lite/assets/kpi-pending.png\" alt=\"\" width=\"40\" height=\"40\">",
        "note": "键 → 文件名见 dist-lite/manifest.json 的 assets，或 core/contract/assets-manifest.ts。当前只有 4 个键（pending / processed / submitted / cc），**没有 kpi-expiring.png 之类** —— 拼错就是一条 404，且页面不会报错、只是图空。要加图先登记再放文件。"
      }
    },
    {
      "name": "Modal",
      "title": "对话框",
      "group": "feedback",
      "status": "stable",
      "desc": "受控对话框。自带 Esc 关闭、点遮罩关闭、`role=\"dialog\"` / `aria-modal` / `aria-label`。**不做焦点陷阱**（focus trap），这是当前已知缺口。",
      "props": [
        { "name": "title", "type": "string", "required": true, "desc": "标题，同时用作 aria-label" },
        { "name": "size", "type": "'sm' | 'lg' | 'xl'", "desc": "尺寸档位。类名 `modal-{size}`" }
      ],
      "slots": [
        { "name": "default", "desc": "主体内容" },
        { "name": "footer", "desc": "底部操作区；**不传则不渲染 footer**" }
      ],
      "events": [{ "name": "close", "payload": "—", "desc": "点击关闭按钮 / 点遮罩 / 按 Esc 时触发。组件自身不维护开关状态，需由父级控制挂载" }],
      "a11y": ["role=\"dialog\" + aria-modal=\"true\" 已就位", "缺少焦点陷阱：打开后 Tab 仍可走到背后页面（待办）"],
      "demos": [
        { "title": "基础用法", "desc": "组件不自己管开合，用 v-if 控制即可。", "code": "<Modal v-if=\"open\" title=\"权限明细\" @close=\"open = false\">\n  正文内容\n  <template #footer>\n    <button class=\"btn btn-secondary\" @click=\"open = false\">取消</button>\n    <button class=\"btn btn-primary\" @click=\"open = false\">确定</button>\n  </template>\n</Modal>" },
        { "title": "尺寸档位", "code": "<Modal v-if=\"open\" size=\"lg\" title=\"申请详情\" @close=\"open = false\">…</Modal>" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"modal-backdrop\" hidden>\n  <div class=\"modal\" role=\"dialog\" aria-modal=\"true\" aria-label=\"权限明细\">\n    <div class=\"modal-header\">\n      <div class=\"modal-title\">权限明细</div>\n      <button type=\"button\" class=\"modal-close\" aria-label=\"关闭\">…</button>\n    </div>\n    <div class=\"modal-body\">…</div>\n    <div class=\"modal-footer\">…</div>\n  </div>\n</div>",
        "note": "结构与角色可 100% 复用（modal-backdrop > modal > header/body/footer，modal-sm|lg|xl）。但**Esc 关闭与点遮罩关闭是行为**，core 层没有、也不会有。原生侧用 hidden 属性控制显隐 + 两三行事件绑定即可；唯一的坑：库给 .modal-backdrop 设了 display:flex，会盖过 hidden 的默认样式，页面里要补一条 [hidden]{display:none}。需要完整的焦点管理就引 vue 层。"
      }
    },
    {
      "name": "OverflowTooltip",
      "title": "溢出提示",
      "group": "feedback",
      "status": "stable",
      "desc": "只在文本**真的被省略号截断**时才弹提示。用 ResizeObserver + 双帧 rAF 测量，避免「没截断也弹」的噪音。底层复用 antd Tooltip，因此 ant-design-vue 是 peerDependency。",
      "props": [
        { "name": "text", "type": "string", "required": true, "desc": "展示文本，同时也是提示内容" },
        { "name": "className", "type": "string", "default": "''", "desc": "容器附加类名（宽度约束由使用方给）" },
        { "name": "force", "type": "boolean", "default": "false", "desc": "为 true 时跳过溢出测量，永远弹提示" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        { "title": "基础用法", "desc": "把容器宽度压窄，鼠标悬停可看到提示。", "code": "<div style=\"width: 160px\">\n  <OverflowTooltip text=\"这是一个很长很长的角色名称会被截断\" />\n</div>" }
      ],
      "native": {
        "available": false,
        "code": "<span style=\"display:block;overflow:hidden;white-space:nowrap;text-overflow:ellipsis\"\n      title=\"这是一个很长很长的角色名称会被截断\">这是一个很长很长的角色名称会被截断</span>",
        "note": "**这一层没有等价物，而且不应该有。** 判断「文本是否真的被省略号截断」要测量 DOM（ResizeObserver + 双帧 rAF），是行为不是样式 —— 把它做进 core，core 就不再是零框架层。原生侧用 title 属性起步：浏览器自带的气泡只在真截断时才会被看到，是静默降级；需要「只在截断时才弹」的精确行为，就引 vue 层（它会带进 ant-design-vue 这个 peer 依赖）。注意库本身也不提供通用的截断工具类（宿主的 Tailwind .truncate 是宿主的能力，别当库的用）。"
      }
    },
    {
      "name": "Pagination",
      "title": "分页",
      "group": "navigation",
      "status": "stable",
      "desc": "列表分页条，含每页条数选择与跳页输入。分页算法固定为**只列前 5 页**（`Math.min(5, totalPages)`），不做省略号折叠；跳页输入框尚未接事件。",
      "props": [
        { "name": "page", "type": "number", "required": true, "desc": "当前页码，受控" },
        { "name": "pageSize", "type": "number", "required": true, "desc": "每页条数，受控" },
        { "name": "total", "type": "number", "required": true, "desc": "总条数" }
      ],
      "slots": [],
      "events": [
        { "name": "update:page", "payload": "number", "desc": "点上一页/下一页/页码时触发" },
        { "name": "update:pageSize", "payload": "number", "desc": "切换每页条数时触发；可选值 10 / 20 / 50" }
      ],
      "demos": [
        { "title": "基础用法", "code": "<Pagination\n  :page=\"page\"\n  :page-size=\"pageSize\"\n  :total=\"total\"\n  @update:page=\"page = $event\"\n  @update:page-size=\"pageSize = $event\"\n/>" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"pagination\">\n  <div class=\"pagination-info\">共 128 条，第 2/13 页</div>\n  <div class=\"pagination-pages\">\n    <button type=\"button\" class=\"page-btn\">上一页</button>\n    <button type=\"button\" class=\"page-btn\">1</button>\n    <button type=\"button\" class=\"page-btn active\">2</button>\n    <button type=\"button\" class=\"page-btn\">下一页</button>\n  </div>\n  <div class=\"pagination-size\">\n    <label>每页</label>\n    <select class=\"select\" aria-label=\"每页条数\"><option>10</option><option selected>20</option><option>50</option></select>\n    <span>条</span>\n  </div>\n  <div class=\"pagination-jump\">\n    <label>跳至</label>\n    <input type=\"number\" class=\"input\" aria-label=\"跳转页码\" style=\"width:64px\">\n    <span>页</span>\n  </div>\n</div>",
        "note": "四块结构固定：pagination-info / pagination-pages（当前页加 .active）/ pagination-size / pagination-jump。翻页是行为：原生页面建议直接走后端路由（?page=2，整页重渲染，零 JS），首尾页用原生 disabled。Vue 侧的「只列前 5 页」算法也属行为，样式层不关心。"
      }
    },
    {
      "name": "PermissionTree",
      "title": "权限树",
      "group": "data",
      "status": "stable",
      "desc": "带展开/收起工具栏的权限树。展开状态由组件内部维护（`Set<string>` 记录折叠项），`nodes` 变化不会重置它。",
      "props": [
        { "name": "nodes", "type": "TreeNode[]", "required": true, "desc": "树根节点。TreeNode = { id: string; name: string; type: string; owned: boolean; children?: TreeNode[] }" }
      ],
      "slots": [],
      "events": [],
      "demos": [
        { "title": "基础用法", "desc": "`owned` 决定该节点渲染「已授权 / 未授权」。", "code": "const nodes: TreeNode[] = [\n  {\n    id: 'sys-1',\n    name: '权限自助门户',\n    type: '系统',\n    owned: true,\n    children: [\n      { id: 'p-1', name: '发起申请', type: '功能', owned: true },\n      { id: 'p-2', name: '审批中心', type: '功能', owned: false },\n    ],\n  },\n]\n\n<PermissionTree :nodes=\"nodes\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"perm-tree\">\n  <div class=\"tree-toolbar\" aria-label=\"权限树操作\">\n    <button type=\"button\" class=\"btn btn-ghost btn-sm tree-toolbar-btn\">展开</button>\n    <button type=\"button\" class=\"btn btn-ghost btn-sm tree-toolbar-btn\">收起</button>\n  </div>\n  <div class=\"tree-list\" role=\"tree\" aria-label=\"权限节点\">\n    <div class=\"tree-row tree-level-1\" role=\"treeitem\" aria-expanded=\"true\">\n      <button type=\"button\" class=\"tree-toggle\" aria-label=\"收起\">▾</button>\n      <span class=\"tree-name\">订单管理</span>\n      <span class=\"tree-type\">模块</span>\n      <span class=\"tree-owned\">已授权</span>\n    </div>\n    <div class=\"tree-children\" role=\"group\">\n      <div class=\"tree-row\" role=\"treeitem\">\n        <span class=\"tree-toggle leaf\" aria-hidden=\"true\"></span>\n        <span class=\"tree-name\">查看数据指标</span>\n        <span class=\"tree-type\">操作</span>\n        <span class=\"tree-denied\">未授权</span>\n      </div>\n    </div>\n  </div>\n</div>",
        "note": "层级与角色由类名结构承担，可 100% 复用：tree-level-{depth} 决定缩进，role=tree/treeitem/group 一并照写。叶子节点必须放空的 .tree-toggle.leaf 占位，否则缩进对不齐。「展开 / 收起」是行为 —— 原生侧可用 <details>/<summary> 承载，类名照旧。⚠️ 工具条按钮上的 **tree-toolbar-btn 是皮肤钩子**：类名由库输出，但样式由宿主 pages.css 提供，属于已登记的唯一一处跨边界耦合。"
      }
    },
    {
      "name": "PermissionTreeRows",
      "title": "权限树行",
      "group": "data",
      "status": "internal",
      "desc": "PermissionTree 的递归子组件，负责渲染一层节点并向下递归。**通常不需要直接使用**；单独导出是为了在只读场景下复用行样式。",
      "props": [
        { "name": "nodes", "type": "TreeNode[]", "required": true, "desc": "本层节点" },
        { "name": "depth", "type": "number", "required": true, "desc": "层级，从 1 起。生成类名 `tree-level-{depth}`" },
        { "name": "collapsed", "type": "Set<string>", "required": true, "desc": "被折叠的节点 id 集合（由父级持有）" }
      ],
      "slots": [],
      "events": [{ "name": "toggle", "payload": "string", "desc": "点击展开/收起箭头，参数为节点 id" }],
      "demos": [
        { "title": "间接使用", "desc": "一般通过 PermissionTree 间接使用。", "code": "<PermissionTree :nodes=\"nodes\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"tree-row\" role=\"treeitem\">\n  <span class=\"tree-toggle leaf\" aria-hidden=\"true\"></span>\n  <span class=\"tree-name\">查看数据指标</span>\n  <span class=\"tree-type\">操作</span>\n  <span class=\"tree-owned\">已授权</span>\n</div>",
        "note": "没有独立用法 —— 它的产物就是权限树里的一行 .tree-row。这恰好说明「递归」是行为、而「行的长相」是样式：组件层做递归，core 层只管这一行长什么样。"
      }
    },
    {
      "name": "PortalMenu",
      "title": "锚点浮层",
      "group": "general",
      "status": "stable",
      "desc": "把任意内容挂到锚点元素下方的浮层容器：Teleport 到 body、跟随锚点定位（滚动/缩放时重算）、点外部关闭。下拉菜单与自定义弹出面板的定位底座。",
      "props": [
        { "name": "open", "type": "boolean", "required": true, "desc": "是否打开（受控）" },
        { "name": "anchorEl", "type": "HTMLElement | null", "desc": "锚点元素，浮层贴在它的下沿" },
        { "name": "width", "type": "number", "desc": "浮层宽度；不传时取 max(minWidth, 锚点宽度)" },
        { "name": "minWidth", "type": "number", "default": "160", "desc": "最小宽度" },
        { "name": "align", "type": "'left' | 'right'", "default": "'left'", "desc": "与锚点的对齐边" },
        { "name": "role", "type": "string", "default": "'listbox'", "desc": "浮层的 ARIA 角色，内容语义变了就改它" }
      ],
      "slots": [{ "name": "default", "desc": "浮层内容，由调用方提供（菜单项、面板等）" }],
      "events": [{ "name": "close", "payload": "—", "desc": "点击浮层与锚点之外的区域时触发" }],
      "a11y": ["`role` 默认 listbox；若内容是菜单，应传 role=\"menu\" 并给子项相应语义"],
      "demos": [
        { "title": "基础用法", "desc": "锚点元素用模板 ref 传入，打开态由调用方持有。", "code": "<button class=\"btn btn-secondary\" ref=\"anchor\" @click=\"open = !open\">更多操作</button>\n\n<PortalMenu :open=\"open\" :anchor-el=\"anchor\" @close=\"open = false\">\n  <button class=\"btn btn-ghost btn-sm\" type=\"button\">导出明细</button>\n  <button class=\"btn btn-ghost btn-sm\" type=\"button\">批量授权</button>\n</PortalMenu>" }
      ],
      "native": {
        "available": true,
        "code": "<button type=\"button\" class=\"btn btn-secondary\" id=\"pm-anchor\">更多操作</button>\n<div class=\"portal-menu\" hidden>\n  <button type=\"button\" class=\"btn btn-ghost btn-sm\">导出明细</button>\n  <button type=\"button\" class=\"btn btn-ghost btn-sm\">批量授权</button>\n</div>",
        "note": "盒子外观 100% 复用（`.portal-menu`：边框 / 圆角 / 背景 / 阴影 / 滚动 / 层级）。**定位是几何而不是配方** —— 原生侧自己算好 `top/left/width` 写进 inline style 即可，Vue 侧这段计算在组件里。展开收起与点外部关闭是行为：`hidden` 属性加几行 JS 就够。"
      }
    },
    {
      "name": "SearchSelect",
      "title": "可搜索单选",
      "group": "form",
      "status": "stable",
      "desc": "输入框式单选：聚焦或点击即展开，输入即过滤，Enter 选中首个匹配，Esc 关闭，已选时悬停把箭头换成清空按钮。菜单 Teleport 到 body 并跟随滚动定位。",
      "props": [
        { "name": "value", "type": "string", "desc": "选中值（配 `@update:value` 使用）" },
        { "name": "options", "type": "SearchSelectOption[]", "required": true, "desc": "选项：{ value: string; label: string; meta?: string }。meta 渲染为右侧小字" },
        { "name": "disabled", "type": "boolean", "default": "false", "desc": "禁用" },
        { "name": "placeholder", "type": "string", "default": "'请选择'", "desc": "占位文案" },
        { "name": "emptyText", "type": "string", "default": "'无匹配结果'", "desc": "无匹配时的提示文案" }
      ],
      "slots": [{ "name": "option", "desc": "自定义选项渲染，作用域参数 { option, selected }" }],
      "events": [{ "name": "update:value", "payload": "string | undefined", "desc": "选中或清空时触发；清空给 undefined" }],
      "a11y": ["输入框是 textbox、菜单是 listbox；输入即过滤。方向键导航未实现（见 knownGaps）"],
      "demos": [
        { "title": "基础用法", "desc": "`meta` 是选项右侧的次要说明。", "code": "const options = [\n  { value: 'admin', label: '项目管理员', meta: '8 项权限' },\n  { value: 'viewer', label: '数据观察员', meta: '3 项权限' },\n]\n\n<SearchSelect v-model:value=\"picked\" :options=\"options\" placeholder=\"请选择角色\" />" },
        { "title": "禁用态", "desc": "禁用时输入框与两个按钮都不可交互。", "code": "<SearchSelect :options=\"options\" disabled placeholder=\"不可编辑\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"search-select\">\n  <input type=\"text\" class=\"input search-select-input\" placeholder=\"请选择\" aria-label=\"选择角色\" />\n  <button type=\"button\" class=\"search-select-arrow\" aria-label=\"展开选项\">\n    <svg width=\"14\" height=\"14\" viewBox=\"0 0 16 16\"><path d=\"M4 6l4 4 4-4\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\"/></svg>\n  </button>\n  <div class=\"search-select-menu\" hidden>\n    <button type=\"button\" class=\"search-select-option selected\"><span class=\"search-select-value\">项目管理员</span><em>8 项权限</em></button>\n    <button type=\"button\" class=\"search-select-option\"><span class=\"search-select-value\">数据观察员</span><em>3 项权限</em></button>\n  </div>\n</div>",
        "note": "输入框直接复用库里的 `.input`（`.search-select-input` 只加右侧留白与文本光标），菜单 / 选项 / 空态 / 清空按钮的位置与配色全在 core 层：菜单是 absolute 定位，贴在 `.search-select`（position: relative）下沿。**过滤、键盘、点外部关闭、跟随滚动都是行为**，原生侧要自己写；展开态用 `hidden` 切换。箭头可用 sprite 的 `yd-icon-chevronDown`；清空按钮的 × **不在 sprite 里**（与 SearchBox 的放大镜同理），需自备。"
      }
    },
    {
      "name": "SingleSelect",
      "title": "单选选择器（按钮式）",
      "group": "form",
      "status": "stable",
      "desc": "按钮触发的单选：点击展开选项列表，选中即回填。与 SearchSelect 的区别是**没有输入框与搜索** —— 选项少时更轻，视觉上是一个触发器而不是表单输入。",
      "props": [
        { "name": "value", "type": "string", "desc": "选中值（配 `@update:value` 使用）" },
        { "name": "options", "type": "SingleSelectOption[]", "required": true, "desc": "选项：{ value: string; label: string }" },
        { "name": "placeholder", "type": "string", "default": "'请选择'", "desc": "未选中时的占位文案" },
        { "name": "disabled", "type": "boolean", "default": "false", "desc": "禁用" }
      ],
      "slots": [],
      "events": [{ "name": "update:value", "payload": "string", "desc": "选中某项时触发" }],
      "a11y": ["触发器是 role=combobox + aria-expanded；菜单是 listbox，选项带 aria-selected。Esc 关闭、Enter/ArrowDown 打开"],
      "demos": [
        { "title": "基础用法", "code": "const options = [\n  { value: 'func', label: '功能角色' },\n  { value: 'data', label: '数据角色' },\n  { value: 'menu', label: '菜单角色' },\n]\n\n<SingleSelect v-model:value=\"picked\" :options=\"options\" placeholder=\"选择角色类型\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"single-select-wrap\">\n  <button type=\"button\" class=\"single-select\" role=\"combobox\" aria-haspopup=\"listbox\" aria-expanded=\"false\">\n    <span class=\"single-select-placeholder\">请选择</span>\n    <span class=\"single-select-chevron\">\n      <svg width=\"14\" height=\"14\" viewBox=\"0 0 16 16\"><path d=\"M4 6l4 4 4-4\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\"/></svg>\n    </span>\n  </button>\n  <div class=\"single-select-menu\" hidden>\n    <button type=\"button\" class=\"single-select-option selected\">功能角色</button>\n    <button type=\"button\" class=\"single-select-option\">数据角色</button>\n  </div>\n</div>",
        "note": "外层 `.single-select-wrap` 提供定位祖先（position: relative），菜单 `.single-select-menu` 是 absolute 贴下沿 —— 这三层结构与 Vue 侧完全一致，照着写即可。展开收起是行为：切 `hidden` + 换 `aria-expanded`。箭头就是图标 sprite 里的 `chevronDown`，原生侧可直接 `<use href=\"…/icons.svg#yd-icon-chevronDown\">`；展开态给触发器加 `.is-open` 会让箭头转 180°。"
      }
    },
    {
      "name": "SelectCard",
      "title": "可勾选卡片",
      "group": "form",
      "status": "stable",
      "desc": "带勾选框的卡片行，可展开详情。整行点击即切换勾选，右侧「详情」开关展开一个详情区（内容走插槽）。用于角色、权限包这类**需要一句说明再决定选不选**的列表。",
      "props": [
        { "name": "checked", "type": "boolean", "required": true, "desc": "是否勾选（配 `@update:checked` 使用）" },
        { "name": "title", "type": "string", "required": true, "desc": "主标题" },
        { "name": "meta", "type": "string", "desc": "次要说明，标题下方的小字" },
        { "name": "details", "type": "string", "desc": "详情兜底文案；给了默认插槽时以插槽为准" },
        { "name": "focused", "type": "boolean", "default": "false", "desc": "聚焦态高亮（键盘导航时由调用方控制）" },
        { "name": "detailsLabel", "type": "string", "default": "'详情'", "desc": "详情开关的文案" }
      ],
      "slots": [{ "name": "default", "desc": "详情区内容，展开时渲染" }],
      "events": [
        { "name": "update:checked", "payload": "boolean", "desc": "勾选态变化（点行或点勾选框）" },
        { "name": "expand", "payload": "boolean", "desc": "详情区展开/收起" }
      ],
      "a11y": ["勾选框是原生 input[type=checkbox]（appearance 自绘），可聚焦、可空格切换；行点击等价于切换勾选", "详情开关带 aria-expanded"],
      "demos": [
        { "title": "基础用法", "desc": "勾选态由调用方持有；`selected` 类名同时给整行上色。", "code": "<SelectCard\n  :checked=\"picked === 'admin'\"\n  title=\"项目管理员\"\n  meta=\"role_admin · 8 项权限\"\n  details=\"可发起、审批与导出全部业务申请\"\n  @update:checked=\"picked = 'admin'\"\n/>" },
        { "title": "详情用插槽", "desc": "详情区可以放任意内容（表格、权限树等）。", "code": "<SelectCard :checked=\"true\" title=\"数据观察员\" meta=\"role_viewer · 3 项权限\">\n  <ChipTree :nodes=\"permNodes\" default-expand-all />\n</SelectCard>" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"select-card selected\">\n  <div class=\"select-card-main\">\n    <input type=\"checkbox\" class=\"select-card-checkbox\" checked />\n    <span class=\"select-card-text\">\n      <span class=\"select-card-name\">项目管理员</span>\n      <span class=\"select-card-meta\">role_admin · 8 项权限</span>\n    </span>\n    <button type=\"button\" class=\"select-card-toggle\" aria-expanded=\"false\">\n      详情\n      <span class=\"select-card-toggle-icon\"><svg width=\"14\" height=\"14\" viewBox=\"0 0 16 16\"><path d=\"M4 6l4 4 4-4\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"1.5\"/></svg></span>\n    </button>\n  </div>\n  <div class=\"select-card-details\" hidden>可发起、审批与导出全部业务申请</div>\n</div>",
        "note": "勾选框是**库内自绘**的（`.select-card-checkbox` 用 appearance:none + :checked::after 画勾），所以原生侧不依赖任何 UI 框架的 checkbox 组件 —— 直接用原生 input 即可。勾选底色由整行的 `.selected` 承担。详情区展开是行为：切 `hidden`，需要箭头旋转就给开关加 `.open`。"
      }
    },
    {
      "name": "ChipTree",
      "title": "叶子芯片树",
      "group": "data",
      "status": "stable",
      "desc": "权限树的一种紧凑渲染：**连续的叶子节点合并成一行芯片**（省纵向空间），分支行带子节点计数徽标，点箭头展开。顶部工具条给总数与「全部展开 / 全部收起」。",
      "props": [
        { "name": "nodes", "type": "ChipTreeNode[]", "required": true, "desc": "树根节点。ChipTreeNode = { id: string; name: string; type?: string; children?: ChipTreeNode[] }（不要求 owned —— 这棵树只展示有权限的项）" },
        { "name": "emptyText", "type": "string", "default": "'暂无数据'", "desc": "无数据时的文案" },
        { "name": "defaultExpandAll", "type": "boolean", "default": "false", "desc": "初始是否全展开；data 异步到货时会在到货后应用一次" },
        { "name": "expandLabel", "type": "string", "default": "'全部展开'", "desc": "工具条展开按钮文案" },
        { "name": "collapseLabel", "type": "string", "default": "'全部收起'", "desc": "工具条收起按钮文案" },
        { "name": "ariaLabel", "type": "string", "default": "'权限节点'", "desc": "树的 aria-label" }
      ],
      "slots": [],
      "events": [],
      "a11y": ["容器 role=tree、分支行 role=treeitem + aria-expanded、叶子组 role=group"],
      "demos": [
        { "title": "基础用法", "desc": "连续叶子自动并成一行；分支行右侧的数字是子节点总数。", "code": "const nodes: ChipTreeNode[] = [\n  {\n    id: 'approval',\n    name: '审批中心',\n    children: [\n      { id: 'p-1', name: '查看数据指标' },\n      { id: 'p-2', name: '导出报表' },\n      { id: 'p-3', name: '批量审批' },\n    ],\n  },\n  { id: 'audit', name: '审计日志' },\n]\n\n<ChipTree :nodes=\"nodes\" />" },
        { "title": "默认全展开", "desc": "适合放在悬浮卡、抽屉这类空间有限且需要一眼看全的位置。", "code": "<ChipTree :nodes=\"nodes\" default-expand-all empty-text=\"该角色暂无已授权权限点\" />" }
      ],
      "native": {
        "available": true,
        "code": "<div class=\"chip-tree\">\n  <div class=\"chip-tree-toolbar\">\n    <span class=\"chip-tree-meta\">6 项</span>\n    <button type=\"button\" class=\"chip-tree-action\">全部展开</button>\n  </div>\n  <div class=\"chip-tree-list\" role=\"tree\" aria-label=\"权限节点\">\n    <div class=\"chip-tree-row chip-tree-depth-1 is-branch\" role=\"treeitem\" aria-expanded=\"false\">\n      <button type=\"button\" class=\"chip-tree-toggle\" aria-label=\"展开\">▸</button>\n      <span class=\"chip-tree-name is-branch\">审批中心</span>\n      <span class=\"chip-tree-count\">3</span>\n    </div>\n    <div class=\"chip-tree-leaf-wrap chip-tree-leaf-depth-2\" role=\"group\">\n      <span class=\"chip-tree-leaf\">查看数据指标</span>\n      <span class=\"chip-tree-leaf\">导出报表</span>\n    </div>\n  </div>\n</div>",
        "note": "缩进不是 inline style，而是契约类名：分支行 `.chip-tree-depth-{1..4}`、叶子行 `.chip-tree-leaf-depth-{1..5}`（Vue 侧同样如此，深度是有限枚举）。展开收起、以及「连续叶子合并成一行」是行为，原生侧静态写就即可。行身份用 `.is-branch` / `.is-leaf` 修饰。"
      }
    },
    {
      "name": "HoverCard",
      "title": "悬停浮层",
      "group": "feedback",
      "status": "stable",
      "desc": "鼠标悬停或键盘聚焦触发器时弹出的信息浮层，移开后延迟消失（给鼠标划过浮层的缓冲）。默认触发器是一个圆形信息按钮，也可以换成任意内容。",
      "props": [
        { "name": "title", "type": "string", "desc": "浮层标题；与 meta 都为空时不渲染头部" },
        { "name": "meta", "type": "string", "desc": "标题下的次要说明" },
        { "name": "triggerLabel", "type": "string", "default": "'查看详情'", "desc": "默认触发器的 aria-label" },
        { "name": "openDelay", "type": "number", "default": "0", "desc": "悬停多久后出现（ms）" },
        { "name": "closeDelay", "type": "number", "default": "140", "desc": "移开后多久消失（ms）" },
        { "name": "disabled", "type": "boolean", "default": "false", "desc": "禁用（默认触发器不响应）" }
      ],
      "slots": [
        { "name": "trigger", "desc": "自定义触发器，替换默认的圆形信息按钮" },
        { "name": "default", "desc": "浮层主体内容" }
      ],
      "events": [{ "name": "update:open", "payload": "boolean", "desc": "浮层显示/隐藏" }],
      "a11y": ["浮层是 role=tooltip", "触发器带 aria-expanded；focusin/focusout 与悬停同样会开关，键盘可达"],
      "demos": [
        { "title": "基础用法", "desc": "默认触发器 + 头部 + 主体内容。", "code": "<HoverCard title=\"项目管理员\" meta=\"仅展示有权限 · 8 项\">\n  <ChipTree :nodes=\"nodes\" default-expand-all />\n</HoverCard>" },
        { "title": "自定义触发器", "desc": "触发器换成任意内容，交互不变。", "code": "<HoverCard title=\"权限明细\">\n  <template #trigger>\n    <span class=\"btn btn-ghost btn-sm\">查看</span>\n  </template>\n  <p>该角色同时持有 3 项敏感类型的查看权限。</p>\n</HoverCard>" }
      ],
      "native": {
        "available": true,
        "code": "<span class=\"hover-card-anchor\">\n  <button type=\"button\" class=\"hover-card-trigger\" aria-label=\"查看详情\" aria-expanded=\"false\">\n    <svg width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><line x1=\"12\" y1=\"16\" x2=\"12\" y2=\"12\"/><line x1=\"12\" y1=\"8\" x2=\"12.01\" y2=\"8\"/></svg>\n  </button>\n</span>\n<div class=\"hover-card\" role=\"tooltip\" hidden style=\"position: fixed; top: 40px; left: 24px\">\n  <div class=\"hover-card-arrow\" aria-hidden=\"true\"></div>\n  <div class=\"hover-card-head\">\n    <div class=\"hover-card-title\">项目管理员</div>\n    <div class=\"hover-card-meta\">仅展示有权限 · 8 项</div>\n  </div>\n  <div class=\"hover-card-body\">…</div>\n</div>",
        "note": "浮层盒子（`.hover-card`：圆角 / 阴影 / 头部渐变 / 滚动区）、触发器（`.hover-card-trigger`）与箭头全是 core 类名。**悬停开关、延迟消失、位置计算是行为**：原生侧切 `hidden` 并按触发器 rect 写 inline `top/left` 即可。浮层层级取 `--z-toast`（要盖过 modal）；箭头按触发器在左上角定位（left: 14px），触发器位置特殊时需要自己挪。"
      }
    }
  ],
  "pending": [
    { "group": "反馈", "items": ["Drawer", "Toast"], "note": "样式已写在库里但无组件实现；P0 §3.3 判为「待建组件」，组件落地时样式随之上移入库" },
    { "group": "数据展示", "items": ["Skeleton", "Timeline", "StatCard", "FileList", "FlowDiagram"], "note": "样式在宿主 layers 里，页面语义尚不明确，暂不迁移" },
    { "group": "通用", "items": ["Chip", "Segment", "DropdownMenu", "Breadcrumb", "UploadZone"], "note": "同上。UploadZone 的 dragover 等交互态样式已写好但从未被任何代码激活（P0 §5.4 已记录）" },
    { "group": "表单", "items": ["FormSelect（antd Select 的主题化薄封装）", "RolePicker（带页签 + 搜索的角色选择浮层）", "RoleAuthDropdown（角色授权下拉）"], "note": "P3 抽取时已在源项目里定位，**刻意缓抽**：FormSelect 绑定 antd Select 的内部 DOM 结构（.ant-select-* 选择器），后两者携带角色域业务语义（读 SYSTEM_MOCK_ROLES 单例、内联 role 字段）。等表单类契约与业务数据边界定下来再抽，避免把宿主字段写进库" }
  ],
  "knownGaps": [
    "Modal 缺焦点陷阱（focus trap）：打开后 Tab 可以走到背后页面",
    "SearchBox 未绑定 v-model / search 事件，目前只能展示",
    "Pagination 的跳页输入框没有接 change 事件；页数超过 5 页不做省略号折叠",
    "host 与库的 `.tag-danger` / `.tag-success` 有样式但无生产者（若不做告警/成功语义可删）",
    "库内组件一律不带 <style>，视觉完全依赖 styles/components.css 的类名 —— 因此脱离库样式加载顺序使用会掉样式",
    "SearchSelect / SingleSelect 的菜单不做翻转定位：贴近视口底部时会溢出（宿主 RolePicker 原型有「空间不够向上弹」的逻辑，P3 抽取时未带上）",
    "SearchSelect 未做方向键导航（上下键移动高亮），宿主原版本也没有",
    "HoverCard 浮层只按触发器左上角定位箭头（.hover-card-arrow 固定 left: 14px），触发点在屏幕右侧时箭头会偏"
  ]
}
